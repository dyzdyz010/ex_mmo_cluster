# 只测试：NPC 决策后端的外部接口凭据。复制为同目录的 .env（已被 .gitignore 忽略）再填值；不要提交真实 key。
# 读取方：apps/gate_server/test/test_helper.exs（live 用例）与 docs/10-active/cross-cutting/tools/ 下的评测脚本。
# 已经在进程环境里设过的同名变量优先，.env 只补缺。格式：每行 KEY=value，不加引号、不加 export，# 开头是注释。

# --- LLM 决策后端（GateServer.Npc.Brain.Llm，OpenAI Responses 线格式）。`mix test --include live_llm` 用。
NPC_LLM_URL=https://api.muteki.site/responses
NPC_LLM_MODEL=gpt-6-astra
NPC_LLM_KEY=

# --- Jev（TypeSafe System One 分类模型）。与它的交互一律用英文。
TYPESAFE_API_URL=https://api.typesafe.ai/v1/systemone
TYPESAFE_MODEL=jev-latest
TYPESAFE_API_KEY=
