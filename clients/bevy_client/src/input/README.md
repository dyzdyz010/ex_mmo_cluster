# Client input module map

- `commands.rs`
  - fixed-step input frame and flag definitions shared by transport and local prediction

This layer should stay small and free of runtime/network side effects.
