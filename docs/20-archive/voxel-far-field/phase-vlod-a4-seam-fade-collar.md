---
status: archived
---

# Phase VLOD-A4：里程碑 A（原始范围）收尾 —— 跨 depth 覆盖性 seam + 换环 fade + L1 collar（并行 A5 顶点瘦身）

> ✅ **本文已归档**：A1-A5 与原始 8km 验收已经收口，但不等于扩展后的里程碑 A、在线 authority cutover 或后续 B/C 已完成。2026-07-12 起，里程碑 A 扩展为完整 XYZ 近/远 LOD 与客户端流送数据流优化；现役实现状态以 A10 / S1b-1 为准，在线 authority cutover 与 B/C 仍未完成。当前实施与退出门槛见 [`2026-07-12-pure-3d-voxel-shell-migration.md`](../../10-active/voxel-far-field/2026-07-12-pure-3d-voxel-shell-migration.md)。

> 承接**里程碑 A（客户端渲染正确，零服务端依赖）**：A1 tier 契约 ✅ / A2 分组件+剔除 ✅ / A3 per-cell greedy merge ✅（quad −57%）＋ Lit-default（额外，远景消色差）。本 phase = **A4（承重）+ A5（可并行）+ A 验收**，收口里程碑 A；过 A 验收后才进里程碑 B（接口冻结）。
> 真值源：架构主稿 [`2026-07-06-voxia-lod-layering-and-technology-design.md`](../../30-reference/overview/2026-07-06-voxia-lod-layering-and-technology-design.md) §3.2/§5/§6/§8/§9；数据源终态=[投影路线](../../30-reference/contracts/2026-07-06-projection-route-final-decision.md)。渲染管线实证：[`2026-07-07-voxia-render-pipeline-camera-lod.md`](../../20-archive/voxel-far-field/2026-07-07-voxia-render-pipeline-camera-lod.md)。

## 0. 一句话

merge 腾出预算后，补齐里程碑 A 剩的三件事：① 跨 depth 环界的**覆盖性 seam 断言**（治 any-solid 膨胀下的重叠型边界）+ 换环 cross-fade（消环界跳变，原 F3）② **L1 collar 3.5m**（把 L0 边缘入带角尺寸 38px→19px）③ **A5 顶点瘦身** 424→~210B/quad + cache LRU。然后 8km 长巡航过 **A 验收**。

## 1. 目标

- **A4 覆盖性 seam**：把现状不验证跨 depth 的 `seam_check` 换成**覆盖性断言**（细侧边界暴露面必被粗侧体积覆盖；小半径全量 + 8km 按环边界抽样）——直接回答原 F3（d4↔d3 裂缝/T-junction 是否可接受）。
- **A4 换环 fade**：cell 换 depth 时 0.2-0.5s 材质 dither cross-fade，保旧 artifact 至新上传完成；observe `ring_reassigned_cells`/`fade_in_flight`。
- **A4 L1 collar 3.5m**：d2-4 加 3.5m collar 档（tier `3.5@4`），depth clamp 1..4→5；merge 后净增约 +56-112k quad（预算可负担）。
- **A5 顶点瘦身**：CPU mesh 424→~210B/quad；persistent artifact cache 加 LRU 淘汰 + 容量上限 + 孤儿清除；RAM 中间态进 observe。
- **近/远视觉一致性（用户 2026-07-07 人肉 A验收 抓出，升为 A 验收硬门槛）**：① 确认 Lit-default 已消除近/远「平亮 vs 受光」色差（旧 Unlit 遗留，`060aaea` 应已修，需 near+far 可见复验）；② **emergence 口径决策**（远景 `EmitQuad(uint16)` 造空 `FEmergenceCell`→无温度/光照发光，近景有——原 F2；base 地形色两侧一致，差在发光与粗材质）；③ **换环无重叠闪烁**（any-solid 膨胀→重叠型边界 z-fighting，A4-3 fade + A4-1 覆盖性 seam 治）；④ 近/远交界 **near-skip suppression 无双显**。
- **远景滑动跟随正确**：远景以 `CenterTile`=**玩家体位置**为中心随玩家滑动（增量重建，实测复用 0.958-0.988）；长巡航验收其跨环/重定心**无缝、无洞、无残影**（这是"玩家走 1km 远景跟着变"的正确性门）。
- **A 验收**：8km WorldGen preview 长巡航（跨 ≥8 tiles）——per-ring quad/内存预算断言、**近/远色彩一致 + 换环无重叠闪烁 + 无洞/无双显 + 滑动跟随无残影**、render_perf 门槛、截图审计（含入带角尺寸 + 近/远交界目视）、cache 淘汰计数 全绿。

## 2. 范围边界（显式非目标）

- **不动服务端**（里程碑 A 零服务端依赖，全 `-VoxiaWorldGenPreview` / fixture 验收）。
- **不做垂直多层（B5）**——维持现状 Y-slab；collar/seam 在单层语义下先落地（垂直 3D 化改 112³ 立方 cell 后 k 需重标定，留 B5）。
- **不做 pages 真消费（B3）**——渲染继续吃预物化 artifact。
- **不重开 raymarch / L4**（defer）；**不自研 SceneProxy**（defer，A2 后未见实测 hitch/显存瓶颈达触发线）。
- **不指望 A4/A5 提 FPS**：本会话实测已定位 **FPS = 像素-bound**（Lumen 全屏 GI ~2-4ms/帧是最大杠杆；merge/瘦身守恒覆盖=只减三角形不减像素，天然不动帧成本）。render_perf 门槛按此认识设定；若不达标是 Lumen 侧（远景降质量/exclude GI，属后续专项/defer），不是 A4/A5 的锅。

## 3. 改动点

### 3.1 A4 覆盖性 seam 断言（承重）
- **背景**：any-solid 规约下粗侧单调膨胀 → 环界是**重叠型非缝隙型**（skirt 治缝不治重叠）。现状 `RunSvoSeamCheck` 只查重复/缺面、**不跨 depth**（原 F3 无实证）。
- **改**：seam 断言从"逐面配对"改**覆盖性**——细侧每个边界暴露面的中心/角点必落在粗侧某实心叶的体积内。落点扩 `RunSvoSeamCheck`（`VoxiaSvoPreview.cpp`）。小半径 automation 全量断言、8km 按环边界抽样。

### 3.2 A4 换环 cross-fade（治环界跳变）
- cell 换 depth（玩家移动跨环）：patch 层加 **0.2-0.5s 材质 dither cross-fade**；保旧 artifact 至新上传完成（现状已保证）。
- observe：`ring_reassigned_cells` / `fade_in_flight`。

### 3.3 A4 L1 collar 3.5m
- tier 配置加 collar 档：`-VoxiaSvoLodRings=3.5@4,7@8,14@24,28@40,56@72`（tier 升一等，见架构 §8）。
- **depth clamp 1..4→5**（3.5m = 112/2^5）。
- **排 merge 后**（预算依赖：无 merge 时 +224k 不可负担；merge 后净增 +56-112k）。L0/L1 边界继续靠 near-skip skirt(24 macro)+underlap+suppression；collar 把入带角尺寸 38px→19px（P-2 跳变款显式豁免，缝隙策略治理）。

### 3.4 A5 顶点瘦身 + cache LRU（可与 A4 并行）
- 顶点格式 **424→~210B/quad**（打包法线/顶点色/UV）。automation 断言顶点数守恒 + 字节量。
- persistent artifact cache：**LRU 淘汰 + 容量上限**；旧 `source_revision` 孤儿 artifact 在 gate 通过后清除。
- RAM 中间态（`FDynamicMesh3`/patch map）纳入 observe 字段。

### 3.5 近/远视觉一致性（用户观察驱动，A4 新增承重）
> 依据：颜色差**非 debug 色**（已 grep 核实无 per-LOD 调试着色）。近景 `EvaluateEmergenceShading(Cell有场数据)`；远景 `EmitQuad(uint16)`→空 `FEmergenceCell`（仅 MaterialId）→同一着色函数。**同 MaterialId → base 色一致**；差异只在 emergence + 材质分辨率。

- **① Lit 一致复验**：`060aaea` 远景已默认 Lit（与近景同 `VoxelMaterial`）。旧 Unlit「平亮 vs 受光」色差应已消——Step 0 near+far 可见复验坐实。
- **② emergence 口径（原 F2，D5 决策）**：远景无温度/光照发光。选项：(a) 远景烘焙简化 emissive（热单元 glow 跨近/远一致，代价=pages/material 需带温度或客户端场采样）；(b) 接受远景无发光 + 文档签收（远景 visual-only，涌现只在近景可见可接受）。base 地形色两侧本就一致，此项只关发光。
- **③ 粗材质签收**：远景每 leaf 单材质（7-56m，any-solid/众数）vs 近景 1m——远景是粗色块，**这是 LOD 的固有代价、非缺陷**，验收只要求「base 色一致 + 换环平滑」，不要求远景细材质。
- **④ near-skip suppression 无双显**：near-skip 只在 L0 覆盖的 Y 层剔远景（`IsSuppressedByNearSkip`，架构 §7 3D 化留 B5）；A4 验收断言近/远交界不双显、不 z-fight（现单层语义下）。

## 4. 决策项

| # | 决策 | 选项 | 倾向 |
| --- | --- | --- | --- |
| D1 | seam 覆盖性抽样粒度 | (a) 小半径全量 + 8km 按环边界抽样；(b) 全量 | **(a)**：全量太贵，按架构 §6/§8 口径 |
| D2 | cross-fade 时长 | 0.2-0.5s | 起步 **0.35s**，真实 RHI 目视调 |
| D3 | 顶点瘦身布局 | 打包到 ~210B/quad 的具体格式 | 施工时定；automation 守恒断言兜底 |
| D4 | Lit-default vs 架构 §3.2 d 材质口径分歧 | (a) 回写架构 doc 对齐；(b) 保留分歧 | **(a)**：Lit 已用户拍板 + 视觉确认，回写架构 §3.2 d（"顶点色三桶 matte/translucent/emissive"→"默认 Lit(VoxelMaterial)，Unlit 经 `-VoxiaSvoFarUnlitMaterial` alt"） |
| D5 | 远景 emergence 口径（原 F2）＋**远景偏暗（A 光照）** | (a) 远景烘焙简化 emissive／抬远景照度（跨近/远一致，代价=pages 带温度/客户端场采样 或 补光改动）；(b) 接受远景无发光 + 偏暗 + 文档签收 | **已拍板 = (b)**（用户 2026-07-08）：远景 visual-only，**照度不管**——2026-07-08 RHI A/B 坐实远景暗主因是光照（无 MDF 收不到 Lumen 天光）、非材质；用户明确不投入抬照度，签收远景偏暗为大气透视/固有代价。热单元跨环"熄灭"同 (b) 接受。(a) 留作后续视觉打磨，不排期 |

## 5. 验收矩阵（A 验收，架构 §8 口径）

| # | 维度 | 断言 | 锚 |
| --- | --- | --- | --- |
| 1 | per-ring 预算 | quad/内存落 §5 预算表 | L1 73-146k / L2 183-275k / L2.5 135-225k / L3 117-195k；collar 净增 +56-112k |
| 2 | 跨 depth 覆盖性 seam | 细侧边界面被粗侧体积覆盖，无洞（小半径全量 + 8km 抽样） | 替代现状不验证跨 depth 的 seam_check |
| 3 | 换环 fade | `ring_reassigned_cells`/`fade_in_flight` 可观测；巡航目视换环不跳 | — |
| 4 | collar 角尺寸 | L0/L1 入带角尺寸 ≤~20px | 截图审计含入带角尺寸目视复核视角 |
| 5 | 顶点瘦身 | 424→~210B/quad；顶点数守恒 | automation 字节/顶点断言 |
| 6 | cache | LRU 淘汰计数 + 容量上限 + 孤儿清除 可观测 | — |
| 7 | render_perf | 8km 长巡航（跨 ≥8 tiles）avg/min FPS 门槛 | **按 Lumen-bound 现实设定**（见 §2）；瓶颈=Lumen 非几何 |
| 8 | 不回归 | `Voxia` automation 全绿 + Build 0 + A2 分组件/StaticDraw/剔除 + A3b merge + Lit-default 不回归 | — |
| 9 | 近/远色彩一致（用户抓出） | near+far 可见 RHI 目视：base 地形色两侧一致（Lit 一致，无"平亮 vs 受光"断层）；emergence 按 D5 口径签收 | 交界视角截图审计 |
| 10 | 换环无重叠闪烁（用户抓出） | 巡航跨环无 z-fighting 闪烁；A4-3 cross-fade 生效可观测；`fade_in_flight` 有值 | any-solid 重叠型边界 |
| 11 | 近/远交界无双显（用户抓出） | near-skip suppression 断言：L0 覆盖处远景被剔、不双显、不 z-fight | `IsSuppressedByNearSkip` |
| 12 | 远景滑动跟随（用户抓出） | 长巡航跨 ≥8 tiles：`CenterTile` 随玩家更新、增量重建、跨环重定心无缝/无洞/无残影 | 复用率 observe |

## 6. 三入口

1. **automation（nullrhi）**：per-ring 预算断言 + 跨 depth 覆盖性 seam 抽样 + 顶点字节守恒 + cache 淘汰计数。
2. **CLI（真实 RHI）**：`until_svo_*` 家族扩 ring 字段；巡航 `sample_render_perf`。
3. **真实操作（可见 RHI）**：飞行长巡航目视无洞/无双显/换环不跳/collar 边缘。

## 7. 工程注意（含本会话实测教训）

- **FPS 归因铁律**：render_perf 不达标先查 **Lumen（~2-4ms/帧，关掉 +36~40 FPS）**，别赖 A4/A5；A5 瘦身赢的是显存/带宽不是 FPS。
- **`far_visible` 遥测 offscreen 卡死=1**（`CountVisibleSvoPartitionedComponents` POV 采集 bug）：巡航/剔除观测须 `--visible-rhi`。剔除本身已证真生效（361→101 可见=28%）。
- **near+far 长巡航流式坑**：`TileWindowRadius=2 + VerticalTileRadius=4` = 61425 chunk 会把 `until_tile_window_full` 拖超时；长巡航验收用更轻近景配置 + 更长超时，或分段。
- **存活纪律**：崩后 `nvidia-smi` 确认恢复、**绝不 `-gpucrashdebugging`**；不碰 raymarch/go-live GPU 路径。
- **机位铁律**：真实 RHI A/B 同 pawn 位=CenterTile；除受测 flag 外命令行一致；引擎 `D:\Epic Games\UE_5.8`、`bUseUnity=false`。
- commit：代码 `clients/Voxia`、文档 `ex_mmo_cluster`；默认不 push；中文注释；全新代码无向后兼容包袱。

## 8. 进度日志

- 2026-07-07：建档。承接里程碑 A（A1/A2/A3+Lit 已收，见 README 阶段表与各 phase 稿）。本 phase = A4（seam/fade/collar 承重）+ A5（瘦身/cache 可并行）+ A 验收。**战略约束（实测钉死）**：FPS=像素-bound，Lumen ~2-4ms 是最大杠杆，A4/A5 不动 FPS、只收几何/显存/视觉正确性。原自造 F3=本稿 A4 seam、F8 白斑(预存 TSR)并入视觉打磨后续、F2 emergence 缝升为本稿 D5/§3.5(A验收硬门槛)、D 顶点瘦身=本稿 A5。
- 2026-07-07（**用户人肉 A验收 抓出四条，全并入本稿**）：用户从 GUI 观察到 ① 近/远色差(经查=旧 Unlit 遗留,`060aaea` Lit-default 应已修,待复验;残差=emergence 无+粗材质,非 debug 色) ② 换环重叠闪烁(=any-solid 膨胀重叠型边界,A4-3 fade 治) ③ 追问近/远交界双显 ④ 追问远景是否随玩家滑动(答:是,CenterTile=玩家体位置增量跟随)。四条已落 §1 目标 / §3.5 / §5 验收#9-12 / §9 Step 0。**尚未开工。**
- 2026-07-08（**Step 0 完成 —— 真实 RHI 目视基线，"破洞"定论=机位假象非 bug**）：`--real-rhi` 离屏 + `--visible-rhi` 可见双路复拍近/远交界（机位 CenterTile macro(1234,-5678)，近场 `-VoxiaTileWindowRadius=1`+远景默认8km四环）。**高空掠射（120–600m 俯冲）三张满是"破洞/破面"→ 用户判"渲染不全"**；经排查**定论为机位假象非渲染 bug**，两独立证据链：① 可见窗口版 == 离屏版（逐张一致，排除离屏 POV 剔除假象——另注 `CountVisibleSvoPartitionedComponents` 只是遥测计数，`far_visible=1` 卡死不驱动显隐，不造洞）；② **眼平自然视角（玩家真实视角）完全干净**、连贯 watertight 无洞。机制=远景 SVO 是**敞底 2.5D 表面壳**（`BuildTileSvoRootBounds` 壳底 `Y0=max(0,MinHeight−CellMacros)`，不实心到地板，省几何设计），高空掠射看到壳的敞开底面 + `-VoxiaNoSky` 背景 clear color 透出=假洞；远处漂浮块=合法浮空岛(`islands=1`)。**① Lit 复验：远景确为 Lit 且正确受光**（有 relief 明暗，非旧 Unlit 平亮）。**真实 backlog（眼平可见）**：(A) 近/远材质色差——远景 `MaterialForBounds` 取 cell 中心→采到地下较暗材质 vs 近景地表沙(+自动曝光放大)；(B) 高空壳敞底——若"飞行/高空俯视"是玩法则需给壳补底/高空另走路径。**②④运动伪影 + D5**(场景无涌现内容判不了)并入 Step 6。教训：目视基线应先用眼平自然视角、勿先高空掠射。**fable5 用量超限，Step 0 与后续以 Opus 顶替。**
- 2026-07-08（**Step 1 完成 —— 跨 depth 覆盖性 seam（承重），直接回答原 F3**）：`RunSvoSeamCheck` 加 ④ 块（`VoxiaSvoPreview.cpp`）：仅 WorldGen 源，对每个细侧 cell 沿"朝更粗邻居"的环界按细叶步长取列，比较**细叶足迹 3×3 any-solid 采样最大列高**(fine 渲染顶) vs **N 侧紧邻粗叶足迹 3×3 采样**(coarse 渲染顶，任一≥HFine即覆盖)——两侧同口径避免"精确列高假阳"。`FVoxiaSvoSeamCheck` 加 `CrossDepthBoundaryFaceCount`/`CrossDepthUncoveredCount` + `SnapshotJson` 两字段(observable)。**关键设计决定：跨 depth 未覆盖不并入 `Status()` pass/fail**——它是 T-junction 细缝的**质量度量**(细侧渲染顶戳出粗侧，被较高侧墙面挡住只余顶边 T-vertex 缝，**非穿透洞**)，由 collar/fade(Step2/3)减小而非要求恒 0；曾误把它 gate 进 Status() 导致 `SvoMergeBudget`/多环 seam 断言连带变红，已回退。**F3 实测（automation d4/d3 环界配置 `1@4,2@3`,radius2）：`boundary_samples=192, uncovered=26`≈13.5% 细侧顶戳出粗侧**；断言=真抽到样本(>0,证非空壳) + `uncovered_ratio<0.5`(any-solid 粗侧覆盖多数=重叠模型成立,松界兜底,collar 后收紧) + 单环下断言不触发(inert)。**验证**：`Build.bat VoxiaEditor` 退出 0(修一处 C4456 `N`→`NTile` 遮蔽);`Voxia.Voxel.SvoPreview` `Result={Success}`(含新断言);其余 18 测全绿(run4)。注:`SvoPreview:335` "composite atlas debug mapping" 是**环境 flaky**(同一 binary run2 pass/run3-4 fail/run5 pass 翻转,与本改动无关,raymarch composite 段未触碰)。代码未 commit(`clients/Voxia`),文档在 `ex_mmo_cluster`。
- 2026-07-08（**Step 2 完成 —— collar 3.5m/depth5 启用（opt-in）+ depth clamp 4→5**）：`MaxLodRingDepth` 4→5(`VoxiaSvoPreview.h`);`ValidateLodRings` 放开 depth5(depth>5 仍拒,超 112/2^k 阶梯)。A1 那段"collar 被拒"测试块翻转为"collar 启用":语法解析 + 映射 depth5 + 叶尺寸 3.5m + `ValidateLodRings` 通过;`radius8` collar build **成功**(`max_depth=5`、`ring0={outer4,depth5}`、出网格、source/farfield ready)+ **净增**(collar quads 152196 > 无collar `7@8` baseline)+ depth5↔depth4 环界 majority-covered。**实测(r8 collar seed1337)**:`cells=280 quads=152196 cross_depth(boundary=2456 uncovered=572≈23.3%<0.5)`。**关键认知**:collar 的 depth5↔depth4 新环界 T-junction 率(23.3%)**高于** depth4↔depth3(13.5%)——finer collar 采到更多尖峰、相邻粗侧更易漏采;**collar 收益不在 far-far 跨 depth 率,而在 near/far 入带角**(近 1m↔远 3.5m 而非 7m,台阶 7×→3.5×,屏 px 38→19,§5#4,归 Step6 真实 RHI 量)——以"更远更不可见的 far-far T-junction 略增"换"最可见近/远入带角减半",净视觉 win。**决定:默认保持 4 环(collar opt-in via 显式 `-VoxiaSvoLodRings=3.5@4,...`)**;翻默认 collar-on 会广泛动其他测试(`SvoMergeBudget` `RingStats==4`、SvoPreview r2 转 depth5 等),留 Step6 A验收(真实 RHI 确认 38→19px + r72 预算 +56-112k)或用户拍板。**验证**:Build 退出 0;`Voxia.Voxel` **19 测全绿**(含新 collar 断言;composite flaky 本轮亦过)。代码未 commit。
- 2026-07-08（**backlog A(材质) 修复 —— 远场逐面贴壁真材质,顶面地表化;并诊断出"远景暗"真因是光照非材质**）：**根因**:远场壳 `MaterialForBounds` 取 leaf **中心 mid-Y** 算单个材质盖六面;跨地表大 leaf 中心落在 soil(`SoilDepthMacro=4`)以下 → 顶面显示**地下料**(surface=1 dirt/subsurface=2 stone,随 leaf 尺寸跳变:7m 叶中心 3.5<4=dirt、14m+ 叶中心≥7=stone) → "材质不统一"。**修**:新增 `MaterialForFace(Context,Bounds,Axis,Sign)`——顶面(+Y)从最顶体素向下扫首层实体(=可见地表 soil)、底面(-Y)从最底向上扫、侧面(±X/±Z)面平面外沿 mid-Y;`EmitSvoLeafSurface` 逐面采(替代 leaf 单值);合并器 `EmitCellFaces` 本就按 MaterialId 分组只并同料(`:958`),**零改动**;seam check `FSvoFaceKey` 只用几何键(`:1828`)不含材质,**无回归**;材质烘成顶点色(`M_VoxelVertexColor` Lit,`VoxiaGreedyMesher:94/102`),**mesh 自包含**、无序列化 bump。SVO node 单值材质(DAG/raymarch)仍走 `MaterialForBounds` 不动。**验证**:Build 退出 0;automation 加材质审计块(按法线分类审 `A.Mesh.Colors`)——`TOP dirt=486 stone=0 other=0`(修前大 leaf 顶面含 stone,现纯 dirt)、`SIDE dirt=2638 stone=1487`(正确分层)、cross-depth `192/26 pass` 无回归;`Voxia.Voxel` **19 测全绿**。**⚠️ 重大发现——材质不是"远景暗"的答案**:真实 RHI(`--visible-rhi` `L_WorldGenSvoPreview` r72 四环,同 collar 机位)出图,远场顶面已全 dirt 但**近亮/远暗观感基本没变**。因近/远**同一个 Lit 材质** `M_VoxelVertexColor`(`VoxiaWorldActor:721/1101`,远景默认 Lit 消色差,`-VoxiaSvoFarUnlitMaterial` 才切 Unlit)+ **同一 dirt 顶点色**却渲染近亮远暗 → **"远景全是褐色"主因=光照/LOD 非材质**:粗 LOD 大块斜俯视多为**竖直侧面**,据 `voxia-render-lighting` 程序化网格竖直崖壁**收不到 Lumen 天光方位补光** → 暗褐;近场细 1m 多朝上顶面迎光 → 亮。材质修复是**真 bug、该修、已修**(移除灰斑 + per-macro 地基),但观感主项归**光照(下一步 A)**。**RHI 谐调坑**:`voxia_stdio_cli.js` 多个 `--cmd` 会**互相覆盖**(`:42` `commandScript=args[++i]`),须**单 `--cmd` 内分号 `;` 串**;SVO 远场须 `L_WorldGenSvoPreview` 图 + `-VoxiaSvoPreview -VoxiaWorldGenPreview -VoxiaSvoTileRadius=72 -VoxiaSvoLodRings=...`(默认 `L_WorldGenPreview` 不开 SVO,`svo enabled:false` 挂死 `until_svo`);`--real-rhi` 离屏 Lumen **GPU OOM 崩**(RHIThread D3D12 out-of-video-memory),截图用 `--visible-rhi` 窗口。代码未 commit。

- 2026-07-08（**当前进度 / 下一步 resume 指针**——用户换电脑,此条为新会话入口）：**已完成并提交推送**(Voxia `master` + 主仓 `master`,本次一起 push)：Step 0(目视基线) / Step 1(跨 depth seam) / Step 2(collar opt-in) / **backlog A(远场逐面贴壁真材质)**。**下一步 = A(光照,用户已拍板"直接开始")**：查远场**竖直侧面为何暗**——候选:①`voxia-render-lighting` 记的"程序化网格竖直崖壁收不到 Lumen 天光方位补光",查补光 RIG 覆盖范围/方位;②固定曝光是否只调好近场;③远场粗 LOD 是否该走更平 shading(减自遮挡);④`M_VoxelFarUnlit`(uniform 亮,`-VoxiaSvoFarUnlitMaterial`)当年为何降 alt(色差?)——这才是"远景全是褐色"真解。**需 `--visible-rhi` 出图**(离屏 `--real-rhi` Lumen GPU OOM,已踩)。**RHI 谐调正确姿势**:单 `--cmd` 分号串 + `L_WorldGenSvoPreview` 图 + `-VoxiaSvoPreview -VoxiaWorldGenPreview -VoxiaSvoTileRadius=72 -VoxiaSvoLodRings=7@8,14@24,28@40,56@72 -VoxiaWorldGenSpawnMacroX=1234 -VoxiaWorldGenSpawnMacroZ=-5678`;机位 `teleport 123450 -567750 8000; look -20 135`。**仍 pending**:Step 3(fade)、Step 4-5(A5 瘦身/cache)、Step 6(A 验收);per-macro 材质升级(地表多材质时)记为后续。
- 2026-07-08（**A（光照）RHI 诊断结论 + D5 拍板 + harness 坑**——用户拍板"照度不管"）：`--visible-rhi` 干净 A/B（Lit vs Unlit，同机位）坐实：**远景真实反照率 = 暖橙土色 + 灰石斑（中等亮度），Lit 把它压成暗褐/冷蓝灰 → "远景暗"主因 = 光照，材质无辜**。机制：远景 mesh 无 mesh distance field → Lumen 天光间接光进不来（见 `clients/Voxia/docs/engineering-notes/2026-06-26-voxel-terrain-black-faces-lumen.md`），只剩太阳 + 4 盏补光 RIG（`VoxiaClientGameMode.cpp:291-329`）直接光，大面/背光面欠照。**用户拍板：照度走 D5(b) 签收（远景 visual-only、偏暗接受），不投入抬照度**。近/远另有真实**材质色相差**（近沙 vs 远土）——独立项，见下条 backlog。**⚠️ harness 坑（务必回写）：`voxia_stdio_cli` 的 `teleport`/`look` 必须先 `fly 1`**，否则被 Pawn 移动 Tick 每帧覆盖（`VoxiaPawn.cpp` Tick `SetActorLocation(Movement->GetPosition())` / `SetControlRotation`）、相机卡出生点（本轮前 6 发全是出生点假象）。正确姿势：`fly 1; wait 800; teleport X Y Z; look pitch yaw`；**Z 单位 = cm**（旧稿"teleport ... 8000"实为 80m）。
- 2026-07-08（**A(光照) 后新 backlog——用户拍板下一步**，替代原"下一步 = A(光照)"）：照度既 D5 签收，下一步转 **3 件实事（均属 A4 / §5#9 收口范围）**：① **窗口内外材质一致**（近 1m 窗 vs 远 SVO 逐面材质在边界不跳，消除采样层不一致，非真实地形差异部分）；② **渐进式合并**（治"一出窗口就大块"，候选：默认开 3.5m collar / 近端环 merge 更保守 / 细化环）；③ **远景地面空洞**排查修复（区分 敞底 2.5D 壳透出 / T-junction 跨 depth 缝 / near-skip 交界缺面 / 真缺 cell，对症）。投研中，改法定后逐件改 + 编译 + automation + RHI 出图验（机位记得带 `fly 1`）。**仍 pending**：Step 3(fade)、Step 4-5(A5)、Step 6(A 验收)。
- 2026-07-08（**backlog ①材质一致 + ②渐进合并 完成——线A/线B 落地，19/19 测全绿 + RHI before/after 视觉验证**）：**线A【重磅根因反转——"近沙远土"色差 = 颜色空间双重解释 bug，非采样、非光照主因】**：全链顶点色契约 = 线性调色板值×255 直存字节（`GreedyMesher ToFColor(bSRGB=false)`）；近窗 PMC 直读字节正确显示奶油沙，而远景 DynamicMesh 转换层 `VoxiaFarFieldDynamicMesh.cpp:40` 用 `FLinearColor(FColor)`（UE **sRGB 解码构造**）把线性字节又解码一次 → dirt (140,102,64)→(0.26,0.13,0.05) 压暗成橙土；stone 同理压成中灰。**修**：改 `SourceColor.ReinterpretAsLinear()`（字节/255 严格逆变换）+ 契约注释；`FarFieldPatchUploaderAutomationTest` 补**非白色** round-trip 断言（白色下两种变换同值测不出，先红后绿）。partitioned+runtime_mesh 两后端同治，HISM/PMC 不受影响。此前"远景真实反照率=橙土"的观察记录系被解码压暗后的假象，一并更正。**线B【渐进合并 = collar 3.5m 翻默认五环】**：主因是 tier 表非合并器——默认四环下窗口边界 1m→7m 一步跳 7×（collar 机制 Step2 已落地只差默认表）；merge 只放大"块感"但形状/颜色守恒，"近端不 merge"零收益纯亏预算。**修**：`FVoxiaSvoBuildConfig::LodRings` + `DefaultLodRings()` 双写点同步改 `3.5@4,7@8,14@24,28@40,56@72`，台阶 3.5×→2×；测试回写：MergeBudget `RingStats==5`、SvoPreview 默认 spec / per-tile 深度(d2-4→5) / EightKmLod cells **72/208/2112/4160/14464** / depth 阶梯 54321 / snapshot lod_config、launch 脚本与各注释同步。**⚠️ 发现真语义冲突：source pages payload=7m mip 与 collar 3.5m 叶不兼容**——pages 测试段（seed 硬编码 `SourcePageCellDepth=4`，复载按五环算 depth5 → cache key 失配载 0）显式 pin 四环 + 注记"pages 源 depth≤4，pages×collar 共存契约归 B3 冻约"。r2 防退化断言改 `QuadCount>MacroCellCount` 口径（`QuadCount`(merge 后) vs `TopQuadCount`(合并前) 跨口径比较在 depth5 下失真）。**验证**：Build `Result: Succeeded`；`Voxia.Voxel` **19/19 全绿**（automation 跑法坑：须 editor 模式跑，`-game` 下 EditorContext 测试不注册报 "No automation tests matched"；此前 composite flaky 本轮亦过）；RHI before/after（`fly 1` 机位，400m look-12 + 250m look-35）：远景回真实浅沙土色、近/远颜色连续、粒度渐进坐实。**代码未 commit。** 下一步 = backlog ③（线C 空洞）：根因已查明 = **cell/环边界覆盖性竖缝**（每 cell 独立 Y 锚定 `BuildTileSvoRootBounds` + 边界面可见性判据与邻侧渲染几何脱耦；`cross_depth_uncovered` 13.5-23.3% 即其量化证据，本稿早前"T-junction 非穿透"的注释对 uncovered 样本不成立需改判）；修法三步 = Y-slab 全局 112m 对齐（对齐 ConfirmedStore 口径）→ 边界面发射判据改邻侧渲染口径（seam check ④ 探测逻辑升级为发射逻辑）→ 叶分类整列精确相交（消 9 列稀疏采样 false-Empty），需 bump `RendererArtifactVersion` + 基线重录。
- 2026-07-08（**backlog ③ 空洞治本一完成——Y-slab 全局对齐落地（Voxia@98692a2），线状竖缝大幅消减**）：`MakeMacroCellArtifact` 内把 cell 全高 bounds 切成 112m 全局对齐 Y-slab、每 slab 一棵 112³ 子树（叶恒 (112/2^k)³ 立方、三轴对齐，统一 ConfirmedStore 口径；全空 slab root 级剪枝；FaceSink 跨 slab 聚合、EmitCellFaces 仍 cell 级一次 merge）。runtime raymarch root 保持全高 bounds（默认关，slab 化留 raymarch 重启评估）。bump `RendererArtifactVersion` 1→2。**验证**：Build Succeeded；19/19 全绿；quad r72 merged 605k→996k（Y 分辨率与 XZ 一致的代价，<1.34M 预算；collar r8 131k→224k）；RHI 同机位对比**线状白缝大幅消减、地形 Y 向细节显著变细**。**⚠️ 观测口径认知**：`cross_depth_uncovered`(26/572) 修后一字不变——该指标是真值场列高 proxy（细侧列高 vs 粗侧 any-solid 包络），**不看树/叶几何**，天然测不出 slab 对齐收益；真验证 = RHI 目视 + 未来"叶 Y1 落全局网格"断言。**残留**：块状白洞（低洼河谷区聚集、平坦成片）——形态疑似 **sea=64 水域未渲染**（WorldGen 水体在 SVO 远景无表现？）而非几何缝，待定性：查 WorldGen 列高在水域的语义 + 洞位置与海平面高度比对；若坐实为水体，归"远景水面表现"新议题（可能 D5 式签收或补平面水面），几何缝路线（Step2 边界发墙/Step3 精确分类）视定性结果再定是否继续。
- 2026-07-09（**backlog ③ 空洞治本三完成——整列精确分类落地（Voxia@aa3a4fc），块状洞消失 + 假山泡沫戳破**）：残留块状洞定性修正：非水域（WorldGen 无水体、低洼仍是 solid 海床），实为 **false-Empty**（27 点稀疏采样 9 列漏山包/谷壁 → 整叶误判 Empty → 地形块缺失）。**修**：`SampleWorldGenExactColumn`（无 envelope Pad 的精确列区间，地形∪浮空岛）+ `ClassifyWorldGenColumnsExact`（叶足迹全列 + Mixed 早退），WorldGen 非 envelope 分类默认走精确版。**⚠️ 重大副作用揭示（忠实度提升，非回归）**：旧采样存在对偶缺陷 **false-Solid**——9 根采样列全为高列时整个 56m 叶被判 Solid、把叶内低谷"填实"，此前远景的"宏伟大山"很大部分是这种假山泡沫；精确分类后山瘦身回真值形状（地平线小起伏），**远景观感变化显著，待用户过目签收**。**验证**：19/19 全绿（wall 377s ≈ 旧 2 倍，留 per-cell 数组缓存优化空间）；RHI r72 近全量（`until_svo_uploaded 900000 20000`）：块状白洞群消失、地形连续，残留零星小白点（T-junction/F8 级，Step2 边界发墙可选收尾）。**RHI 取证坑**：精确分类后全量 build 变慢，`until_svo_uploaded` 默认 minCells=1 会拍到"半成品远景"（山区未 build 完 = 山缺失假象），必须显式传 minCells（如 20000）等近全量再截图。**仍 pending**：Step 3(fade)、Step 4-5(A5)、Step 6(A 验收)；backlog③ 可选收尾 = Step2 边界发墙（治零星 T-junction 残留）+ 分类性能优化（TMap→per-cell 数组）。
- 2026-07-09（**backlog③ 治本二完成 + 山更高调参 + 空洞终局定性——五 commit 全绿收口**）：①**治本二（Voxia@bef3151）**：`FaceOutsideHasAir` 侧面精确化（face 外一排列逐列区间判定，部分暴露发全墙=补缝；**顶/底保留 mid 单点**——精确化顶面会把"地表高于叶带"的被埋顶面发出来 → 采地下 stone、材质审计红 85，已实测回退）＋ 跨 ring 边界 skirt（`CoarserRingBoundarySkirtMacros`，细侧贴更粗邻环的侧面向下 skirt=邻粗叶带高；`FSvoBuildContext` 增 LodRings）。②**山更高（Voxia@cb49208，用户拍板）**：preview `MountainAmplitude 1400→1800`、`MaxHeightMacro 1600→2400`（防 clamp 削顶）——**仅客户端 preview**，与服务端 Rust 既有分岔口径一致（lowland 340vs150 / mask 0.50-0.82vs0.62-0.9 已是先例），服务端 canonical 不动；四向 RHI 扫图 y270 现明显尖峰。③**空洞终局定性 + 修复（Voxia@34c3b1b）**：精确分类后残留圆弧状白洞 = **列高 clamp 0 的深谷露世界底**（lowland=64+(Base-0.5)×340 → 谷底 -106→clamp 0 → 列区间空 → 无几何），此前被 false-Solid 假地形掩盖；`MinHeightMacro 0→4` 保底，y0 洞区 RHI 复验全消。**每步 Build Succeeded + 19/19 全绿**。GoldenParity 为 wire 协议 parity 与 WorldGen 调参无关；WorldGen 测试为结构性断言不锚定列高。backlog ①②③ 至此全部收口；剩 A4 主线 Step3(fade)/Step4-5(A5)/Step6(A 验收) 与可选性能优化（分类 TMap→数组，automation 377→419s 可接受）。
- 2026-07-09（**Step 3（A4 换环 cross-fade）完成 —— dither 材质 + 新旧组件对互补淡入淡出（Voxia@64891fb 基建 + @0e56460 接线）**）：换环重建 patch 由「同组件原地 `SetMesh`」改为「新建组件淡入 + 旧组件淡出 + 完成销毁旧」，仅 `RingReassignedCells` 映射的 patch 走 fade（普通 dirty patch 仍原地更新，零额外成本）。**三层落地**：① `FVoxiaFarFieldFadeController` 纯逻辑状态机（`StartFade/AdvanceTo/CancelSection/CancelAll`，线性互补 α、重入保护、in-flight 计数；含 nullrhi automation `Voxia.Voxel.FarFieldFadeController`）；② `M_VoxelFarDither`（`Lit + BLEND_Masked + DitherTemporalAA`，`OpacityMask←ScalarParameter "FadeAlpha"`，稳态 α=1 恒过 = 与 Opaque 等价零代价；python 幂等生成脚本 `create_far_dither_material.py`，**commandlet 下引擎 Function 须 `unreal.load_asset` 直载**，`EditorAssetLibrary` 走 Asset Registry 找不到）；③ `AVoxiaWorldActor` 接线：build 侧 `RingReassignedCells` 标记 + `SnapshotJson` 新增 `ring_reassigned_cells`；上传侧换环 patch 摘旧组件→Acquire 新建→双 MID（新 α=0 淡入 / 旧 α=1 淡出）→`Tick` 内 `AdvanceSvoPartitionedFades` 推进（不受 0.25s 轮询节流）→完成销毁旧组件 + 新组件回稳态材质；`Remove`/`Clear` 路径联动 `Cancel` 防泄漏；材质缺失自动降级单帧切换。默认 0.35s，`-VoxiaSvoFadeSeconds=`（clamp 0.05-5.0）/`-VoxiaSvoNoFade`。**验证**：Build Succeeded；`Voxia.Voxel` **20/20 全绿**（新增 FadeController 测，19→20）；RHI 换环序列截图确认换环后稳态干净无重叠残影（动态 fade 的 GIF 目视归 Step6，本轮录屏受 Windows 锁屏干扰未成，不阻塞）。**可选未做**：`fade_in_flight`/`fades_started_total` 透传 SnapshotJson（签名改动，低优）。
- 2026-07-09（**worldgen 起伏调参（用户第三轮"太平"）—— 山更高更频繁 + 平原更起伏（Voxia@be4c2be）**）：300m 俯视下 ±170m lowland 缓丘读作一片平，山脉又因 mask 门槛窄（近半地图 mask<0.5 纯 lowland）+ `Ridge^2.2` 强压而稀疏低矮。**五杠杆协同**（非单顶一数）：`GVoxiaLowlandAmplitude 340→420`；山脉门槛 `[0.50,0.82]→[0.38,0.66]`（开山区 ~50%→~62% 且更早饱和到满高）；`GVoxiaRidgePower 2.2→1.6`（松开脊线中段压制）；`GVoxiaMountainAmplitude 1800→2600`；`MaxHeightMacro 2400→3600` 留头避免高峰被 clamp 削平（峰值上限约 lowland 峰 ~274 + 2600 ≈ 2874m < 3600 cap；`VoxiaMacroSizeCm=100` → 1 macro=1m）。仅动 `-VoxiaWorldGenPreview` 开发预览生成器（2026-07-06 终态裁决：永久 dev-preview/fixture 源，不进生产通路），服务端 canonical 未触。**⚠️ cache-key 隐患记账**：`BuildKeyPrefix`（`VoxiaSvoSourcePages.cpp:457`）worldgen 只哈希 `Config` 结构体字段（含 `MaxHeightMacro`）——本轮因同时改 `MaxHeightMacro` 键自动失效强制重建；但 lowland/mountain 振幅、mask、ridge 这些 `.cpp` 文件级 constexpr **不在 cache key**，纯调形状常量会静默命中旧缓存，归 **Step5 缓存卫生**（考虑加 worldgen shape version 或把形状常量并入键）。**验证**：Build Succeeded；`Voxia.Voxel` **20/20 全绿**（含 `SvoMergeBudget` quad 预算 / `SvoPreview` / `WorldGenV1`，taller 地形下 quad 仍在阈内）；RHI 双机位（3000m look-45 俯瞰 + 1200m look-12 平视）确认山峰高耸出平原、平原缓丘起伏、盆地/河谷穿插延伸至地平线，用户已过目。**仍 pending**：Step 4-5(A5 顶点瘦身 / cache LRU)、Step 6(A 验收长巡航)。
- 2026-07-09（**Step 4（A5 顶点瘦身）完成 —— 远景 SVO 网格 424→91B/quad（-78.5%）+ v3 格式（Voxia@7990d96 基建 + @ebe10ad 翻转 + @94651fa 审查加固）**）：**分工=fable5 出详细实施设计 + fable5 独立代码审查，主会话 Opus 实施+裁决**（用户拍板的模型路由）。新紧凑类型 `FVoxiaFarMeshData`{Positions FVector3f / Triangles / Colors FColor / QuadFaceDir uint8 / QuadMaterial uint16}：法线/UV0 由 QuadFaceDir+顶点位置、UV1/UV2 emissive 由 QuadMaterial 经同一 `EvaluateEmergenceShading` 纯函数**确定性重建**（远景面全轴对齐→无损，SinkCm=0），不入盘；上传 PMC 前经 `ExpandToLegacyMeshData` 逃生门重建（每 patch 一次，非每帧）。**关键设计纠偏**（fable5 勘察，作者假设之外）：① legacy 无 tangent 数组（重建 no-op）；② UV1/UV2 是 emissive 非朝向可重建→须 +2B 存 QuadMaterial（89→91B）；③ VHI 与 SVO 曾共用 `FVoxiaFarFieldPatchUploader`→模板化 `TVoxiaFarFieldPatchUploader<T>`+双 using alias 拆开（VHI 零语义变化）。**近场/heightmap/VHI 链全程 legacy 零改动**。序列化 v3：`SerializeMesh` 紧凑 5 数组、`ArtifactVersion`+`RendererArtifactVersion` 双常量 2→3（破坏性，cache key 含版本→旧 artifact 自动失效重建）、manifest 硬门拒旧 fixture。**fable5 审查裁决可 push，5 条改进已落地**：LoadArtifact 值域校验（`FVoxiaFarMeshData::IsStructurallyValid`——v3 起盘上数据首次当索引用，堵坏 artifact 驱动 `Normal[Dir/2]` 越界写的新 UB 面）、EmitQuad 轴序 `check`、删死码（legacy SplitMeshByPatch/TileForQuad/AppendQuad+`FVoxiaFarFieldPatchSplit`，无调用方；legacy AppendMesh 留供 VHI/抑制）、补测（A-4 v3 roundtrip+值域守卫、新 `Voxia.Voxel.FarFieldCompactPatchUploader`）、float ±671km 包络注释。**验证**：Build Succeeded；`Voxia.Voxel` **22/22 全绿**（含新 90 例等价性测试、材质审计经 expand 端到端验证、紧凑 uploader）；**RHI 双后端**：默认 PartitionedDynamicMesh（直读紧凑）与翻转前 wg_overview **视觉完全一致**（几何+颜色字节保真），PMC（唯一走运行时 expand）同地形受光正确（重建法线无误）。内存收益全链兑现（artifact/BuildResult/patch/uploader≈4.7× 缩减；PMC Expanded 局部即弃不缓存）。**遗留 follow-up**（不阻塞）：on-disk source_pages fixture 需经 smoke 脚本重生成（v2→v3，仅影响 source_pages 冒烟，非 automation/RHI）；`fade_in_flight` 透传 SnapshotJson 低优。**仍 pending**：Step 5(A5 cache LRU + 顺带修 worldgen 形状常量不在 cache-key 隐患)、Step 6(A 验收长巡航)。
- 2026-07-09（**后置备忘：collar × source-pages × 紧凑 artifact 接口矛盾——归 B3（服务端接入），本轮不解**）：用户要求记下并后置。**矛盾**：collar 是最内圈 3.5m/depth5 细环（`3.5@4`，Step2 落地、Step4 后默认开），而投影路线终态（[`../../30-reference/contracts/2026-07-06-projection-route-final-decision.md`](../../30-reference/contracts/2026-07-06-projection-route-final-decision.md) §2）下**远区数据源 = source pages（7m=depth≤4 mip 投影）**——7m 源信息**造不出 3.5m 细节**，故连接态下 collar 与 pages 不能直接共存。**三个候选解**（B3 冻接口时择一/组合）：① collar 只在**有 1m 近窗数据覆盖**处开（近窗能补细节），纯 pages 远区退回 depth4；② source pages 也出 depth5 层（服务端多发一档，带宽/存储成本上升）；③ 连接态**关 collar**（近→远台阶回到 1m→7m，牺牲入带角平滑）。**当前不阻塞**：本地 WorldGen 预览能直接产 depth5，Step4/collar/Step5 均在 WorldGen 源下推进；矛盾只在**接服务器（B3/里程碑 C）**时兑现。**与 Step5 的耦合**：连接态 source_revision churn（编辑/涌现/设计师内容滚 revision）会高频改 cache key，Step5 的 LRU/孤儿清理正为此设计——B3 定 collar 方案时需与 Step5 缓存键口径一并核。**动作**：B3 开工前先派 fable5 出"source pages 接入 × collar × 紧凑 artifact"衔接调研 + 方案,再实施。
- 2026-07-09（**里程碑 A 当前状态 / 续作 resume 指针**——本轮收口,新会话入口）：**已完成并推送**（Voxia `master`@94651fa + 主仓 `master`@4095f53）：Step 0（目视基线）/ Step 1（跨 depth seam）/ Step 2（collar opt-in→默认开）/ Step 3（换环 cross-fade）/ **Step 4（A5 顶点瘦身 424→91B/quad）** + backlog ①②③（材质一致/渐进合并/空洞治本）+ worldgen 起伏调参。**下一步 = Step 5（A5 cache 卫生）**：给 `FVoxiaSvoPersistentArtifactCache` 加 ① LRU + 容量上限（mtime touch 作 LRU 真值，`-VoxiaSvoArtifactCacheMaxMB=` 默认 ~2GiB，不进 cache key）② `RunMaintenance` 孤儿目录清除（`prefix_meta.json` 记活跃前缀）③ 顺带修 worldgen 形状常量（lowland/mountain 振幅、mask、ridge）不在 cache key 的静默命中坑（并入键或加 worldgen shape version）+ observe 字段。**价值**：连接态 revision churn 下磁盘有界（生产相关）+ 消除"改 worldgen 却命中旧缓存"的 dev 陷阱。**再下一步 = Step 6（A 验收）**：8km 长巡航真实 `--visible-rhi`（§5 #1-#12 全绿 + fade 动态 GIF 目视——上轮录屏被 Windows 锁屏干扰未成,需解锁重录）+ 回写架构 §3.2d 材质口径（D4）。**模型路由（用户拍板）**：重思考/路线调研/算法架构设计/代码审查/问题排查 → **fable5**；实施/精确编辑/裁决 → 主会话 Opus。**RHI 取证锚**：`L_WorldGenSvoPreview` + `-VoxiaSvoPreview -VoxiaWorldGenPreview -VoxiaSvoTileRadius=72 -VoxiaWorldGenSpawnMacroX=1234 -VoxiaWorldGenSpawnMacroZ=-5678`；机位命令前必带 `fly 1; wait 800` 再 teleport/look（Pawn Tick 每帧覆盖机位）；`until_svo_uploaded 900000 20000` 等近全量再截图（精确分类后 build 慢，默认 minCells=1 会拍半成品）;PMC 后端 `-VoxiaSvoRenderBackend=ProceduralMeshSection`;构建=`Build.bat VoxiaEditor Win64 Development`,automation=editor 模式（去 `-game`）跑 `Automation RunTests Voxia.Voxel`。**后置**：collar × source-pages 接口（上一条,归 B3）。

- 2026-07-09（**Step 5（A5 cache 卫生）完成——worldgen shape 入键 + LRU 容量 + 孤儿清理（Voxia@035b8fa 主体 + @2b77166 minor 收尾，已推送）**）：**分工=fable5 设计→fable5 对抗加固→Opus 实施→fable5 代码审查（用户拍板路由）**。三件事：**① worldgen 形状常量入 cache key**——新增 `FVoxiaWorldGenShapeConstants`（`VoxiaWorldGenV1.h`）从 `.cpp` 匿名 namespace **具名 constexpr 单一真值构造**（19 标量 + 2 octave 数组 + hoist 出的 `GVoxiaMountainMask/RidgeSeedOffset`，禁抄字面量）+ `HashShapeConstants`（double 经 `FMemory::Memcpy` bit-cast）+ `ShapeSignature()`（magic static 缓存）；`BuildKeyPrefix` 委托 `BuildKeyPrefixWithShape` 注入。消除"改 lowland/mountain/mask/ridge 等 constexpr 却静默命中旧缓存"（2026-07-09 记账的 dev 陷阱）。**顺带修既有潜伏 bug（fable5 加固揪出）**：`BuildKeyPrefix` 的 worldgen printf 用 `%d` 吃 `Seed`(int64)/`ChunkVersion`(uint64) → 只读低 32 位，高位变化不换 key（同类缺陷），改 `%lld/%llu`。**② LRU + 容量上限**——`-VoxiaSvoArtifactCacheMaxMB`（默认 2048=2GiB）**策略旋钮不进 cache key、不进 `SameBuildConfig`/`SameReusableSvoConfig`**；mtime 作 LRU 真值（`LoadArtifact` 命中**节流 1h** `SetTimeStamp`，避免纯命中 21k-build 逐文件开句柄）；`RunMaintenance` 收尾 sweep（先孤儿后 LRU，结构化三层枚举 `IterateDirectoryStat` 单趟拿 size+mtime）；**自噬护栏 = 本次 build 触及集（load∪save∪内存复用分支的 `ArtifactPath`）绝不淘汰**，超 cap 只 `bCapacityExceededByProtectedSet` warn+observe **不 fail build**；`cap≤0` 才硬失败（H-gate，CLI+build 双入口）。**③ 孤儿前缀清除**——`<root>/prefix_meta.json` 记活跃前缀 `last_seen`（ticks 以字符串存防 double 精度丢失）；TTL 7 天；**关键正确性（fable5 加固 CI-1/CI-3）**：`LastSeen = max(meta last_seen, 叶内最新 .vsvoart mtime)` **两信号取强者**（防热前缀 meta 陈旧被误杀）；**meta 缺失/损坏 = adoption 轮**——禁一切 TTL 删除 + 把枚举到的前缀**全量 grandfather 到 now**（防 Step5 上线**首个 sweep 误删所有"7 天没被写过"的历史前缀**，因旧代码 `LoadArtifact` 从不 touch mtime → 盘上 mtime=写入时间）。删除严格限 root 子树内三层结构（`scene_*/cv/16hex`），meta 写 temp+`Move(bDoNotRetryOrError=true)` 近原子。**⚠️ 对设计的正当偏差（fable5 审查已复核正确）**：原设计分 `TouchPrefixMeta`（每 build 轻量写 meta）+ `RunMaintenance`（条件 sweep）两方法，实施发现"先 touch 写窄 meta 后 sweep 再读 meta"会掩盖 meta 缺失信号→adoption 失效→重蹈误删，故**合一**为单一 meta 读写权威（`RunMaintenance` 始终跑，内部读一次 meta 判 adoption/前缀已知→决定是否重枚举 sweep→末尾统一写一次）。**observe**：SnapshotJson +11 键（`worldgen_shape_signature`/`artifact_cache_max_bytes`/`maintenance_ran`/`total_disk_bytes`/`evicted_*`/`orphan_*`/`capacity_exceeded_by_protected_set`/`maintenance_error`），`SanitizePathToken` 补剥 `.` 防 `..` 穿透。**验证**：`Build.bat VoxiaEditor` Succeeded；`Automation RunTests Voxia.Voxel` **22/22 全绿**（新增 `Voxia.Voxel.SvoCacheHygiene` T1-T10：shape 逐字段敏感性+wiring 两段证明绕过 constexpr 运行时不可改 / LRU 淘汰 / mtime touch 反转 LRU 序 / 自噬护栏 / 孤儿 TTL+adoption grandfather / cap 校验 / 节流 / 逃生门 / 策略字段不进 key；**21 既有测试无回归**含 SvoPreview/SvoMergeBudget/GoldenParity/WorldGenV1，证 shape 入键+config 字段零副作用）。**T6 一处测试假红修正**（fable5 审查独立命中同因）：adoption 把 P1 grandfather 到 now，run2 须把 `NowUtc` 推进 8 天越过 7 天租约才触发 TTL 删——是**实现的 grandfather 正确行为**，改测试非改代码。**minor 收尾（fable5 审查 9 项）**：**已修 7**——空骨架删除查返回值(m-1)、`prefix_meta` ticks 值域钳制(m-7)、T1 补 MountainOctaves Wavelength 扰动(m-8)、T5/T9 pin 四环省时(m-9) 均在 `035b8fa`；observe `maintenance_ran`→`swept` 消歧义(m-2，`bMaintenanceRan`→`bSwept` + JSON 键 `artifact_cache_swept`)、空 `cv`/`scene` 父目录 best-effort 回收(m-5，Tree=false 仅空目录)、默认 2GiB 收敛到 `FVoxiaSvoPersistentArtifactCache::DefaultMaxTotalBytes` 单一真值(m-6) 在 `2b77166`。**设计有意保持记账 2**（非 bug）：shape 签名对 source_pages 源也无条件入键（改 worldgen 常量后离线 pages fixture 需重烘焙，B3 接服务端时重审键派生输入，m-3）；`SanitizePathToken` 剥 `.` 使带点 content_version 目录首 sweep 一次性 orphan（安全方向、预期非误删，m-4）。**仍 pending**：Step 6（A 验收长巡航）。
- 2026-07-09（**里程碑 A 当前状态 / 续作 resume 指针**——Step 5 收口后更新）：**已完成**：Step 0-4 + backlog①②③ + worldgen 调参（Voxia@94651fa / 主仓@4095f53）+ **Step 5（A5 cache 卫生，Voxia@035b8fa+@2b77166，已推送）**。**下一步 = Step 6（A 验收）**：8km 长巡航真实 `--visible-rhi`（§5 #1-#12 全绿 + fade 动态 GIF 目视——上轮录屏被 Windows 锁屏干扰未成，需解锁重录）+ 回写架构 §3.2d 材质口径（D4）。Step 6 是里程碑 A 最后一步，过后进里程碑 B（接口冻结）。**模型路由（用户拍板）**：重思考/路线调研/算法架构设计/代码审查/问题排查 → **fable5**；实施/精确编辑/裁决 → 主会话 Opus。**RHI 取证锚**：`L_WorldGenSvoPreview` + `-VoxiaSvoPreview -VoxiaWorldGenPreview -VoxiaSvoTileRadius=72 -VoxiaWorldGenSpawnMacroX=1234 -VoxiaWorldGenSpawnMacroZ=-5678`；机位命令前必带 `fly 1; wait 800` 再 teleport/look；`until_svo_uploaded 900000 20000` 等近全量再截图；PMC 后端 `-VoxiaSvoRenderBackend=ProceduralMeshSection`；构建=`Build.bat VoxiaEditor Win64 Development`，automation=editor 模式（去 `-game`，`-nullrhi -nosound`）跑 `Automation RunTests Voxia.Voxel`（退出 255=有测试失败，读 `Saved/Logs/Voxia.log` 的 `Result={Fail}`）。**Step 5 新增 cache 卫生 flag**（Step 6 长巡航可用）：`-VoxiaSvoArtifactCacheMaxMB=<int>`（默认 2048）/`-VoxiaSvoCacheNoMaintenance`（逃生门）/`-VoxiaSvoCacheForceSweep`。**后置**：collar × source-pages 接口（归 B3，B3 开工前派 fable5 出衔接调研）。

- 2026-07-09（**Step 6（A 验收）完成 —— 里程碑 A 收口**；发布饥饿架构修复为本轮最硬一仗；Voxia@a3ca4f6 + 主仓@本提交，一起 push）：**分工=fable5 诊断/设计/审查、Opus 实施/裁决（用户拍板路由）**。四条主线：**① fade observable 透传（矩阵 #3/#10 "fade_in_flight 有值"）**：`fade_in_flight`/`fades_started_total` 从 `AVoxiaWorldActor::SvoFadeController` 沿既有 `SvoFar*` 上报路径透传进 `svo` SnapshotJson（尾插，Printf 说明符/实参 fable5 程序化核实 141=141）；fable5 审查揪出"fade 完成后 Tick 路径不重报→值冻结残值",补 `AdvanceSvoPartitionedFades` 完成分支重报使其真实归零。**② 发布饥饿架构修复（承重）**：8km 长巡航（跨 tile 11→19）首次暴露 **SVO 连续巡航 publish starvation 活锁**——pawn 移动持续触发重定心请求、build 也在递进 center 启动,但 `FVoxiaFarFieldBuildPipeline` supersede-discard 语义把每个完成 build 在 apply 前丢弃(有更新 pending 即整体重启),复用基底只取最后 apply 结果 → 呈现永冻出生 build（center 冻 [11,0,-51]、reused/reassign/fades_started 恒 0）,阻塞 #12/#3/#10。**observe 铁证**：`voxia_svo_build_started`×18（center 11→18 递进）/`superseded`×14/`tiles_built`×2（仅出生 apply）。**latent defect 非回归**（连续巡航 SVO 跟随此前从未被 RHI/automation 验证,0.958 复用出自 stop-and-wait 单跳冒烟）。**修复**：**fix1 发布语义**改"无条件先 apply（`SvoPreview=MoveTemp`+`MarkApplied` 推进 revision→WorldActor 0.25s poll 触发 `QueueSvoMeshUpdate`→换环 fade/ring-reassign 自然发生）再链下一个 pending"（`StartSvoBuildAsync` 完成回调,事件 `superseded`→`chained`）;**fix2 source-kind revision 绑定**（`PipelineSourceRevision = ConfirmedVoxelStore?VoxelRevision:0`,消除近景窗口流式 `++VoxelRevision` churn 污染 WorldGen build 身份、防 apply 后空转活锁）。**VHI 同构缺陷本轮只加 TODO 记账**（D-10 冻结/不在验收路径/fix1 须配 fix2 否则 spin,复活时照 SVO 模板修）。**③ quad 漂移 → 签名修复**：新增 `Voxia.Voxel.SvoSlidingFollow`（radius6 连续 re-center 锁不变量）暴露**跨 ring 边界增量 quad 比全量多 0.07-0.2%**。fable5 诊断根因=`SvoTileSignature`（复用门 + **磁盘 artifact cache 键**共用判别）只折 near-skip 位、**漏折更粗邻居环深**——cross-ring skirt 按邻居环深发射且不进 greedy merge,中心移动使邻居跨 ring 换深而本 cell 未换深时旧签名不变 → 陈旧 skirt 误复用（内存漂移,自愈）/ 错误 artifact 命中（持久 cache 键碰撞,非自愈）。**性质=A 良性**（MissingFace=0 无洞/DuplicateFace=0 无双显/覆盖表面一致/步长递减收敛,re-cruise 视觉确认无 artifact）,但因兼任 cache 键故**机制层修**：`SvoTileSignature` 折入"更粗邻居环深"（`NeighborDepth<SelfDepth`,与发射端 `Context.MaxDepth=cell 深度` 同条件;4bit/邻居精确位打包位8-23;内部同深 cell 不折保高复用）→ 归零漂移让测试保持精确 + 关掉 cache 键碰撞。**治本共享**（fable5 审查揪出 `DebugSvoTileSignature` 旧副本漏折环深、CLI fixture radius≥5 存/取键分叉）：导出 `FVoxiaSvoPreview::TileBoundarySignature` 单一实现,删 Debug 两副本函数路由到共享（符合仓库既有"防 artifact key 漂移"纪律）。**④ 验收 #1-#12 结果**：`Automation RunTests Voxia` **41/41 全绿 + Build 0**（新增 SvoSlidingFollow;SvoPreview/SvoMergeBudget/SvoCacheHygiene/FarMeshData/FarFieldFadeController 无回归）。逐项：**#1** per-ring（cells 72/208/2112/4160/14464、depth54321、macro 21016、SvoMergeBudget 预算断言过;post-merge r72≈1.9M 因 be4c2be 山更高调参,测试自身预算=活门槛,§5 旧带宽待复核记账）;**#2** seam（automation `boundary=192 uncovered=37 pass` 19% + collar r8 33% + RHI seam pass 无洞）;**#3** fade observable（`fade_in_flight=18` 活值 + `fades_started_total` 44→153 递增 + FadeController 测）;**#4** collar 入带角（tier 7m→3.5m **结构性减半=入带角 38→19px≤20px** + automation collar tier 存在 + RHI A/B collar-on 更细）;**#5** 瘦身（FarMeshData 91B/quad）;**#6** cache（SvoCacheHygiene T1-T10）;**#7** render_perf（**720p avg 178-195 FPS/min 142-187,远超 Lumen-bound 地板 40/25**）;**#8** 不回归（41/41+Build0+A2 partitioned/static + A3b merge + Lit-default）;**#9** 近/远色彩一致（交界眼平 + **150/400/800/1500m 抬高远景**目视:Lit-default 沙/褐色相全距离一致,无平亮 vs 受光断层;远处偏暗=D5(b) 签收 Lumen 无 MDF）;**#10** 换环无闪烁（re-cruise reassign 936/624/312 + fade + 无 z-fight）;**#11** 无双显（交界俯视目视近远干净分层 + `near_skip_radius=1`/`macro=21016`）;**#12** 滑动跟随（**re-cruise 修复后 center 11→14→16→17 随玩家前进 + reused 0.934→0.978 + 8 chained/0 superseded + 抬高远景 leg0 vs leg8 目视 shifted**）。**RHI 取证锚补充**：`--visible-rhi` + `L_WorldGenSvoPreview` + r72 8km + **400m 抬高浅俯 look-22（近→远 LOD 连续无洞,比贴地眼平更能代表远景全貌）** + 交界 land 眼平 + collar r24 A/B。**⑤ fable5 后续项（非阻塞,记账）**：① `SvoSlidingFollow` 是 builder 层锁,transport 回调 apply-then-chain 时序由 pipeline 原语测试（`FarFieldBuildPipeline` "same pending→no supersede"）+ A 验收 re-cruise 端到端兜底,可补 transport 层时序测试;② `MatchesCompletedSvoBuild` 漏拷 `bVisualEnvelope`/`bFarMergeEnabled`（仅 `-VoxiaSvoVisualEnvelope`/`-VoxiaSvoFarNoMerge` 两非默认 flag 下 UseCompleted 失效,无 spin/无正确性问题,非验收路径）;③ `chained` 事件可补 revision 字段（confirmed 排障）;④ VHI 同构发布饥饿修复（复活 VHI 时）。**⑥ 用户反馈下一任务**：可视客户端"体素块闪烁 + 屏边白色渐变"（TSR/后处理方向,非光照,非本轮引入,疑与早记 "F8 白斑(预存 TSR)" 同源）。**§3.2d D4 材质口径已回写**架构主稿。**GIF 动态 fade 目视**：#3/#10 已由 `fade_in_flight` 活值 + `fades_started` 递增 + re-cruise 无 z-fight 坐实,动态 GIF 作可选补充（需解锁屏幕,本轮未录,不阻塞收口）。
- 2026-07-09（**里程碑 A 收口 / 续作 resume 指针**——Step 6 完成,新会话入口）：**里程碑 A（客户端渲染正确、零服务端依赖）全部收口**：A1 tier ✅ / A2 分组件+剔除 ✅ / A3 greedy merge + Lit-default ✅ / **A4（seam/collar/换环 fade/发布饥饿修复）✅** / **A5（顶点瘦身 424→91B/quad / cache 卫生）✅** / **A 验收 #1-#12 全绿 ✅**。两仓 = origin/master（Voxia@a3ca4f6 + 主仓@本提交,一起 push）。**下一步 = 里程碑 B（接口冻结）**;B3（服务端 source-pages 接入）开工前先解 **collar × source-pages 接口矛盾**（7m pages 造不出 3.5m collar,归 B3,派 fable5 出衔接调研）。**并行插队候选（用户拍板下一任务）= 修可视客户端"体素闪烁 + 屏边白渐变"**（TSR/后处理,用默认 Lumen/平行光,非光照特化）。**模型路由（用户拍板,持续有效）**：重思考/路线调研/算法架构设计/代码审查/问题排查 → **fable5**;实施/精确编辑/裁决 → 主会话 Opus。

## 9. 施工 step（每 step：编译 + 最小 automation + 进度日志；默认不 push）

- **Step 0（当前状态 near+far 可见基线）**：**先看清起点再动手**——用**轻近景配置**（`TileWindowRadius=1` + 短 `VerticalTileRadius` + 长 `until_tile_window_full` 超时，绕开 61425-chunk 流式超时坑）跑一次 near+far 可见 RHI，截交界视角，肉眼记录：Lit-default 是否已消色差①、换环重叠闪烁现状②、近/远交界双显④、滑动跟随（移动几 tile 复验）。产出 A4 的目视基线 + D5 拍板依据。**不写码，纯观测**。
- **Step 1（A4 seam）**：`RunSvoSeamCheck` 扩跨 depth 覆盖性抽样 + automation 断言（小半径全量）。
- **Step 2（A4 collar）**：tier 加 `3.5@4` + depth clamp →5 + 预算复核；automation per-ring 落带。
- **Step 3（A4 fade）✅ 完成**（Voxia@64891fb+@0e56460，见 §8 2026-07-09）：patch cross-fade + `ring_reassigned_cells` observe（`fade_in_flight`/`fades_started_total` 透传 SnapshotJson 可选未做）。
- **Step 4（A5 瘦身）✅ 完成**（Voxia@7990d96+@ebe10ad+@94651fa，见 §8 2026-07-09）：紧凑 `FVoxiaFarMeshData` 424→**91B/quad**（-78.5%，比设计初估 ~210B 更省）+ 90 例逐字段等价 automation + fable5 设计/审查、双后端 RHI 验证。
- **Step 5（A5 cache）✅ 完成**（Voxia@035b8fa+@2b77166，见 §8 2026-07-09）：worldgen shape 入键（+ 修既有 %d 吃 int64/uint64 bug）+ LRU 容量上限（mtime 真值/节流 touch/自噬护栏）+ 孤儿清除（prefix_meta.json/max(meta,mtime) 取强者/adoption 防首 sweep 误删）+ observe 11 键 + CLI 3 flag；`Voxia.Voxel` 22/22 全绿（新增 SvoCacheHygiene T1-T10）；fable5 设计→加固→审查。
- **Step 6（A 验收）✅ 完成**（Voxia@a3ca4f6 + 主仓，见 §8 2026-07-09）：8km 长巡航真实 RHI 过 §5 #1-#12（含**发布饥饿修复**=SVO 连续跟随、fade observable 透传、cache 键签名折邻居环深）；`Automation RunTests Voxia` 41/41；回写架构 §3.2d 材质口径（D4）。**里程碑 A 收口，下一步里程碑 B（接口冻结）。**
