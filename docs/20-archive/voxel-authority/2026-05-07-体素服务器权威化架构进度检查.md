# 2026-05-07 体素服务器权威化架构进度检查

本文记录当前体素链路相对下一阶段目标的真实进度和架构缺口。目标不是给局部代码打补丁，而是明确哪些基础设施必须先成为服务端权威链路的一部分，避免客户端和服务端场景继续分叉。

更新（2026-05-16）：`ex_mmo_cluster` 的局部场底座已在 Phase 6 落地；Phase 7.A `FieldKernel` kernel-first 迁移已完成，Phase 7.D 温度异常入口已验证材料属性驱动的升温/降温骨架。后续推进以 `docs/docs/10-active/field-emergence/2026-05-16-phase7-local-field-runtime-roadmap.md` 为准，先补 `SetTemperature/Cool`、`FieldSource` 生命周期和 `FieldEffect` 写回边界，再推进 `ConductionPathKernel`。

## 当前目标

1. 体素全面服务器权威化：所有 confirmed voxel truth 只能来自服务端 snapshot、delta、object/catalog/result，不允许遗漏其他本地写入路径。
2. 网页客户端左右键增减体素体能力完善：macro cell 和 prefab/refined micro cell 一视同仁，都可以摆放和破坏。
3. 为体素格填充属性：第一步覆盖温度、湿度、密度和导热性，并为后续燃烧、冻结、结构、魔法传播留出权威规则空间。Phase 5 / 6 已完成，Phase 7.A 已把场传播算法改成 `FieldKernel` kernel-first 调度，Phase 7.D 已接通温度异常入口；后续重点先转向正式温度设置、source 生命周期和 field effect 写回边界。

## 当前真实进度

当前主线已经达到 **server-authoritative macro voxel MVP**：

- 网页客户端默认 `voxel_sync=server-authoritative`，在线模式下 `WorldStore` 只应用服务端 `ChunkSnapshot / ChunkDelta / VoxelIntentResult`。
- `VoxelImpactIntent` 已能提交整宏格放置和破坏。
- `PrefabPlaceIntent` v1 已能把服务端 catalog 中的 macro blueprint 放到场景里。
- dev seed、中心 chunk 订阅、HUD / CLI / observe 验证面已经可用。

但这还不是全面权威化：

- refined micro 数据仍主要是浏览器离线路径能力。
- 服务端 snapshot 协议虽然定义了 `RefinedCells / AttributeSets / TagSets / ObjectRefs / EnvironmentSummaries` section，但 refined/catalog 目前仍是空池或预留字段。
- Gate 对 `VoxelImpactIntent` 的语义仍是 macro-only：`impact_kind == 0` 表示破坏，非 0 表示 material id 并写整宏格。
- `PrefabPlaceIntent` v1 是逐 macro cell 写入，失败不回滚，不具备跨 chunk 原子性。
- 属性链路只有温度/湿度的局部字段和环境摘要雏形，密度/导热性还没有类型化属性目录、存储和 delta 规则。

## 代码证据

| 位置 | 当前行为 | 架构含义 |
| --- | --- | --- |
| `clients/web_client/src/voxel/onlineVoxelWorldAdapter.ts` | `placeBlock` / `breakBlock` 发送 `sendVoxelImpactMacro`；`placeMicroBlock` / `breakMicroBlock` 返回 `micro_*_not_supported_by_server` | 在线网页路径只支持 macro 编辑，不支持服务端 micro 编辑。 |
| `apps/gate_server/lib/gate_server/worker/ws_connection.ex` / `tcp_connection.ex` | `target_world_micro` 被 floor 到 world macro；`impact_kind` 被解释为 break sentinel 或 material id | 当前 intent 语义无法表达 micro slot、object part、属性写入或 refined patch。 |
| `apps/scene_server/lib/scene_server/voxel/chunk_process.ex` | `normalize_operation/1` 只接受 `put_solid_block` 和 `break_block` | Scene 侧热 chunk 只有整宏格写入和清空操作。 |
| `apps/scene_server/lib/scene_server/voxel/codec.ex` | 文档注释说明 refined cells 和 catalogs section 目前限制为空池 | wire contract 还没有真正承载 refined truth 和 attribute/tag truth。 |
| `apps/scene_server/lib/scene_server/voxel/prefab_raster.ex` | rasterize 输出 `(chunk_coord, local_macro, NormalBlockData)`，且 v1 明确排除 microgrid/refined/boundary/socket snap | 服务端 prefab v1 是 macro blueprint，不是 refined prefab。 |
| `apps/scene_server/lib/scene_server/voxel/storage.ex` | storage 有 `refined_cells / attribute_sets / tag_sets` 字段，但类型仍是 `term()` / raw list | 字段存在不等于权威模型存在；缺 typed domain、validator、codec、delta 和 persistence 语义。 |
| `apps/scene_server/lib/scene_server/voxel/normal_block_data.ex` | `temperature_delta / moisture_delta / attribute_set_ref / tag_set_ref` 已预留 | 温度/湿度有局部入口，密度/导热性应通过 attribute set/catalog 建模。 |
| `apps/scene_server/lib/scene_server/voxel/macro_environment_summary.ex` | `default/current temperature` 与 `default/current moisture` 已建模 | 环境摘要可承载动态温湿度，但不是完整属性系统或模拟器。 |

## 目标一：全面服务器权威化缺口

### 缺口 A：canonical truth 还没有覆盖所有 cell mode

设计文档要求 `ChunkSnapshot` 包含：

- `MacroHeaders`
- `NormalBlocks`
- `RefinedCells`
- `AttributeSets`
- `TagSets`
- `EnvironmentSummaries`
- `ObjectRefs`

当前只有 macro headers、normal blocks 和部分 environment summaries 真正进入 encode/decode 热路径。refined cells、attribute sets、tag sets、object refs 仍没有完整类型和 wire payload。

需要补齐：

- `RefinedCellData` 服务端 domain module，包括 occupancy mask、micro material、micro state、micro part、owner object provenance。
- `AttributeSet` / `TagSet` 服务端 domain module 和 catalog snapshot/patch。
- `ChunkObjectRef` 从摘要结构升级为可支持 object/part/provenance 查询的权威引用层。
- chunk hash / cell hash 覆盖 refined、attribute、tag、environment、object ref，不能只覆盖 normal block。
- DataService 持久化必须保存同一份 canonical snapshot，不允许只保存 macro 子集。

> **Phase 1.2 进展（2026-05-13）**：`AttributeSet` typed domain 已实现，section `0x04` wire codec 落地。
> `apps/scene_server/lib/scene_server/voxel/attribute_set.ex` + `attribute_entry.ex` 提供 typed value bag
> （value_type ∈ `{i16, u16, fixed32 Q16.16, enum8, bitset32}`）；`Storage.intern_attribute_set/2` 提供
> 1-indexed 稳定 ref（0 = null），pool 内按 byte-wise canonical 排序。
> 设计与 8 个决策点见 `docs/docs/20-archive/voxel-authority/2026-05-13-phase1-attribute-set-typed-domain.md`（D-1..D-8 推荐方案
> 全部生效；schema_version 未 bump，3 个 pinned chunk_hash baseline 字节级稳定）。
> 该 commit 仅动 `attribute_sets`；`tag_sets` 仍是 `[term()]` 空池，留给 Phase 1.3。
>
> **Phase 1.3 进展（2026-05-13）**：`TagSet` typed domain **已实现**，section `0x05` wire codec 落地。
> `apps/scene_server/lib/scene_server/voxel/tag_set.ex` 提供 typed set-membership pool（`tag_ids: [u32]`，
> 扁平 u32 无 namespace，纯集合成员资格、不携带 value）；`Storage.intern_tag_set/2` 提供
> 1-indexed 稳定 ref（0 = null），pool 内按 byte-wise canonical 排序。Wire layout
> `set_count:u32 / sets[]{ tag_count:u16, tag_ids[u32] }`。设计与 4 个决策点见
> `docs/docs/20-archive/voxel-authority/2026-05-13-phase1-tag-set-typed-domain.md`（T-1..T-4 推荐方案全部生效；
> schema_version 未 bump，3 个 pinned chunk_hash baseline 字节级稳定，新增 26 个 tag_set 测试 0 回归）。
> 该 commit 仅动 `tag_sets`；`CatalogPatch` 留给 Phase 1.4。
>
> **Phase 1.4 进展（2026-05-13）**：`CatalogPatch` envelope **已实现**（opcode `0x71`，
> 见 `apps/scene_server/lib/scene_server/voxel/catalog_patch.ex`）。
> envelope 含 `schema_kind:u8 / base_version:u64 / new_version:u64 / op_count:u16 /
> ops[]{ op_kind:u8, entry_id:u32, payload_len:u16, payload:bytes }`；payload 内容
> Phase 5 落地 `AttributeDefinition` / `TagDefinition` 时再解释。Forward-compat：
> 未知 `op_kind` decoder 保留 raw payload（pass-through byte-stable），未知
> `schema_kind` 硬错误（envelope-level dispatch tag）。opcode 槽位 `0x71`：
> 设计草案推荐 `0x6F` 与生产 `VoxelDebugProbe` 冲突，用户改判 `0x71`；voxel 段
> 扩展到 `0x60..0x7F`。设计与 3 个决策点见
> `docs/docs/20-archive/voxel-authority/2026-05-13-phase1-catalog-patch-minimum.md`（P-1..P-3 推荐方案全部生效，
> opcode 实际值由 0x6F 改 0x71）。新增 36 个 catalog_patch 测试 0 回归，
> 3 个 pinned chunk_hash baseline 字节级稳定（CatalogPatch 不动 chunk truth）。
> 本 commit 仅动 scene 侧 envelope；gate codec 集成 / 客户端 decoder 留给 Phase 5。

### 缺口 B：离线路径需要硬隔离

浏览器离线 microgrid/refined prefab 能力是有价值的调试面，但它不能在在线模式中伪装成 MMO 权威真相。

需要保持的边界：

- `VITE_VOXEL_SYNC=offline` 才允许本地写 confirmed voxel storage。
- 在线模式下所有编辑只能发 intent；本地只能做 preview / ghost / pending UI。
- CLI / HUD / observe 必须继续显示 `voxel_sync`、`voxel_truth_source`、last snapshot/delta/result 和 edit rejection reason。
- 回归测试要覆盖在线 adapter 不调用本地 refined mutator。

### 缺口 C：跨 chunk 一致性还不是事务模型

**状态：已实现（2026-05-13 verifier 审计通过 Phase 3 §3.2 三条验收口径）**。本段保留作为历史背景。当前实际链路：Gate `tcp_connection.ex` `run_prefab_transaction` 三分支（`single_chunk_fast_path` / `same_owner_fast_path` / `coordinator_begin_transaction`）；World `TransactionCoordinator` + `TransactionExecutor` 全在线（含 Phase 3-bis :prepared resume）；Scene `BuildTransactionApplier` + `ChunkProcess.pending_fence` + `voxel_chunk_pending_transactions` 持久化；prepare 失败自动 `rollback_prepared`，commit 部分失败由 Watcher reload 持久化 fence + executor :prepared fast path 恢复。详见 `apps/scene_server/lib/scene_server/voxel/audit/2026-05-13-phase3-verifier-audit.md`。

当前 Gate 的 prefab v1 是 cell-major loop：每个 cell 单独 route chunk 并调用 `ChunkDirectory.apply_intent`。如果前面写成功、后面失败，已写入部分不会回滚。

这会直接破坏“不要遗留小尾巴导致场景不一致”的目标。

需要把 prefab/build/edit 多 cell 写入改为：

1. Gate 只做协议 decode、鉴权状态检查和请求转发。
2. World 负责确定涉及的 chunks、lease、parcel/build permission、transaction id。
3. Scene `BuildTransactionApplier` 对各 chunk prepare。
4. 全部 prepare 成功后 commit；任一失败则 abort 已 prepared chunk。
5. `VoxelIntentResult` 返回 authoritative summary，客户端只消费 result + delta/snapshot。

## 目标二：macro 与 prefab micro 一视同仁的编辑缺口

### 缺口 A：服务端没有 refined 写操作

**状态：已实现核心（2026-05-13 verifier 审计通过）**。本段保留作历史背景。`put_micro_block` / `put_micro_blocks`（Phase A1-1b batch）/ `clear_micro_block` 已落地（`storage.ex` 177-324），含 empty↔refined 转换、layer 合并、refined→empty 降级、solid 拒绝、占用冲突检测；`CellRefined` `delta_kind=2` 下发链路 `chunk_process.ex` 2295-2334 完整。`merge_refined_cell` / `split_solid_macro_to_refined` / `clear_refined_cell_if_empty` / `collapse_full_refined_to_solid` 列入 Phase 3+ follow-up（见 Phase 2 audit §6）。

当前 Scene 支持：

- `put_solid_block`
- `break_block`

下一阶段至少需要：

- `put_micro_block`
- `clear_micro_block`
- `merge_refined_cell`
- `split_solid_macro_to_refined`
- `clear_refined_cell_if_empty`
- 可选的 `collapse_full_refined_to_solid`

这些操作必须在 Scene 侧完成冲突检查和版本推进，并通过 `CellRefined` delta 下发。

### 缺口 B：intent 需要类型化，不能继续挤在 impact_kind 里

**状态：已实现核心（2026-05-13 verifier 审计通过）**。本段保留作历史背景。typed `VoxelEditIntent` v1 已落地（`apps/gate_server/lib/gate_server/codec.ex` 616-701），15 字段：`request_id / client_intent_seq / logical_scene_id / action / target_granularity / target_world_micro / face_normal / material_id / blueprint_ref / object_ref / part_ref / attribute_patch_ref / expected_chunk_version / expected_cell_hash / client_hint_hash`。`action` ∈ `{0=place, 1=break, 2=damage, 3=replace, 4=attribute_patch}`、`target_granularity` ∈ `{0=macro, 1=micro, 2=object_part}`。Gate WS / TCP dispatch 已支持 `action × granularity` 映射到 Scene operation（`ws_connection.ex` 1086-1120）。

macro-only 的 `VoxelImpactIntent` 可以保留为兼容路径，但下一阶段应该引入或升级为 typed edit intent：

```text
VoxelEditIntent {
  request_id
  logical_scene_id
  actor_cid
  action              // place | break | damage | replace | attribute_patch
  target_granularity  // macro | micro | object_part
  target_world_micro
  face_normal
  material_id?
  blueprint_ref?
  object_ref?
  part_ref?
  attribute_patch_ref?
  expected_chunk_version?
  expected_cell_hash?
}
```

这样左键/右键可以统一表达：

- 命中普通 macro：破坏 macro 或在邻接 macro 放置。
- 命中 refined micro：破坏 micro slot 或在邻接 micro slot 放置。
- 命中 prefab part：按 owner object + part 结算局部破坏。
- 选中 prefab：提交 blueprint/object placement，而不是客户端先改 chunk truth。

### 缺口 C：prefab server rasterizer 仍是 macro blueprint

**状态：部分实现，事务化已完成 / refined patch + object refs 未真实化（2026-05-13 verifier 审计）**。Phase 3 §3.2 三条验收口径已通过：事务化机制（World coordinator + Scene applier + Phase 3-bis 持久化）完整；`BlueprintCatalog` 已升级 v2 single-macro micro mask（3 个 builtin）。但本段所列的 `PrefabRasterPatch` 结构、跨 macro refined blueprint、`PrefabRaster.layer_attrs` 扩展（attribute_set_ref / tag_set_ref / owner_object_id / owner_part_id）仍是 follow-up，列入 Phase 5 prefab raster 真实化范畴（Phase 4 已就绪 ObjectRegistry / ChunkProcess 一侧的 provenance）。详见 Phase 3 audit §6 F-1/F-2/F-3。

服务端 `PrefabRaster` v1 只把 blueprint cell 写成 `NormalBlockData`。要支持 refined prefab，需要 rasterizer 输出 per-chunk patch：

```text
PrefabRasterPatch {
  chunk_coord
  local_macro
  refined_cell_patch
  object_ref_patch
  attribute_set_refs
  tag_set_refs
}
```

同时要补：

- rotation 对 macro offset 和 micro occupancy 的一致变换。
- boundary snap / socket snap 的服务端校验版本。
- overlap check 基于服务端 canonical chunk truth。
- prefab instance / object instance 的 id 分配和 covered chunks 记录。

### 缺口 D：局部破坏需要 provenance

**状态：已实现（2026-05-13 verifier 审计通过）**。本段保留作为历史背景，实际落地能力见 README Phase 4 / 4-bis / A4 / A4-4 段。

浏览器本地已有 `microPartIds` 概念，但文档已经指出它不足以支持嵌套 prefab 和多 object 共处同一 macro。服务端需要每个 occupied micro slot 能反查：

- owner object id
- assembly id 或可反查 assembly 的压缩引用
- part id
- part tags / affordances
- runtime state，例如 health、damage mask、open/closed、burning、ownership

没有 provenance，左键破坏 prefab 微格只能变成“清一个 slot”或“清整个 macro”，无法进入后续战斗、魔法、资源和结构系统。

## 目标三：温度、湿度、密度、导热性属性缺口

**已实现**（2026-05-13 Phase 5 全收口，Phase 5.A-5.F 全部 done）。

- 缺口 A 属性目录：5 个 typed attribute 落地（temperature / humidity / moisture
  / density / thermal_conductivity，Phase 5.A-5.C 全部实现）。
- 缺口 B 属性存储粒度：5 层 merge_rule 实施 4 层（L1 / L2 / L3 / L5；L4
  object-part 推到 Phase 5.D.2 或更晚），Phase 5.D 已落 `effective_attribute_at`
  高层 API。
- 缺口 C 模拟器和边界传播：Phase 5.E 已落 simulation tick 基础设施
  (`Simulator` behaviour + `SimulationTick` + dirty bounds + lease/dirty/no-simulator
  fence + deterministic output_hash + 拉模式邻区占位)，Phase 5.F 已落
  `DiffusionSimulator`（3D 7-stencil 温度 / 湿度扩散，参数化 attribute_name /
  α / dt，Neumann 边界 fallback）+ `EnvironmentUpdated` (opcode 0x72) wire
  payload codec + ChunkProcess fanout。Phase 5.F 本 commit 不修改
  `storage.environment_summaries` canonical truth，simulator writeback +
  multi-instance runtime 注册推到 Phase 5.F.runtime；客户端 TS decoder
  推后续（草案 F-7 推荐）。

### 当前已有基础

- `NormalBlockData` 已有 `temperature_delta` 和 `moisture_delta`。
- `NormalBlockData` 已有 `attribute_set_ref` 和 `tag_set_ref`。
- `MacroEnvironmentSummary` 已有 default/current temperature 和 moisture。
- Web material catalog 已能把 temperature/moisture delta 用于表现色调。

### 还缺的基础设施

### 缺口 A：属性目录

需要明确 attribute catalog 的第一版 schema：

```text
AttributeDefinition {
  id
  name
  unit
  value_type        // i16 | u16 | fixed32 | enum | bitset
  default_value
  min_value
  max_value
  merge_rule        // override | add_delta | max | min | material_default
  dynamic?          // true 表示运行时可变
}
```

**AttributeCatalogSnapshot envelope 已实现**（Phase 5.A，2026-05-13）：服务端
wire 类型 `SceneServer.Voxel.AttributeCatalogSnapshot`（opcode `0x6E`，全量
catalog 快照通道）+ `SceneServer.Voxel.AttributeDefinition` 完整字段校验
落地，与 Phase 1.4 `CatalogPatch` envelope（opcode `0x71`，增量通道）并存。
Phase 1.2 chunk-local `AttributeEntry.key_id` 在本 commit 后语义升级为
catalog 全局 `AttributeDefinition.id`（wire 字段不变）。设计草案
`docs/docs/20-archive/field-emergence/2026-05-13-phase5a-attribute-catalog-snapshot.md`，A-1..A-6 全部
推荐方案。第一批 typed attribute 注入（temperature / humidity / density /
thermal_conductivity）由 Phase 5.C 接续。

**TagCatalogSnapshot envelope 已实现**（Phase 5.B，2026-05-13，第一批 tag 注入由 Phase 5.C 接续）：
服务端 wire 类型 `SceneServer.Voxel.TagCatalogSnapshot`（opcode `0x6D`，
全量 tag catalog 快照通道）+ `SceneServer.Voxel.TagDefinition`（仅 `id + name`，
比 `AttributeDefinition` 简单）完整字段校验落地，与 Phase 1.4 `CatalogPatch`
envelope（opcode `0x71`，`schema_kind=0x02` tag 增量通道）并存。Phase 1.3
chunk-local `TagSet.tag_ids` 中的每个 u32 元素在本 commit 后语义升级为
catalog 全局 `TagDefinition.id`（wire 字段不变）。设计决策直接沿用
Phase 1.3 T-1..T-4 + Phase 5.A A-1..A-2（无新决策点）。
具体 commit hash 见 `git log --oneline | grep phase5b`。
第一批 typed tag 注入（flammable / conductive / 等）由 Phase 5.C 接续。

**第一批 attribute / tag catalog seed + in-memory runtime 已实现**（Phase 5.C，2026-05-13）：
落地 5 个 typed attribute（`temperature` add_delta dynamic / `humidity` add_delta dynamic /
`moisture` add_delta dynamic / `density` material_default static / `thermal_conductivity`
material_default static，全部 fixed32 Q16.16）+ 8 个 typed tag（`flammable` / `conductive` /
`wet` / `frozen` / `burning` / `magical` / `structural` / `transparent`）。seed 走
`apps/scene_server/priv/catalogs/attribute_catalog_v1.exs` / `tag_catalog_v1.exs`
（.exs Elixir 字面量，C-4）；runtime 由 `SceneServer.Voxel.AttributeCatalog` /
`SceneServer.Voxel.TagCatalog` GenServer + private ETS 提供（C-5），挂在
`SceneServer.VoxelSup` children 第一/二位（C-6，确保 ChunkProcess 启动前就绪）。
设计草案 `docs/docs/20-archive/field-emergence/2026-05-13-phase5c-first-batch-catalog-seed.md` C-1..C-8 全部
推荐方案。具体 commit hash 见 `git log --oneline | grep phase5c`。
Catalog 持久化（DataService schema 跨进程重启恢复）推到 Phase 5.C.2；五层
merge_rule 实施 + refined cell 的 per-MicroLayer attribute 路径推到 Phase 5.D；
客户端 catalog 消费（web_client TS decoder for opcode `0x6E` / `0x6D` + UI）由
Phase 5.D / 5.E 真正下发 catalog 时一并落地。

第一批建议：

| 属性 | 建议归属 | 原因 |
| --- | --- | --- |
| temperature | dynamic environment/current field | 会随规则帧扩散和变化。 |
| humidity / moisture | dynamic environment/current field + material response | 会受环境、液体、材料吸湿影响。 |
| density | material/static attribute，必要时 cell override | 更像材质/结构参数，不应每帧扩散。 |
| thermal_conductivity | material/static attribute，参与 heat diffusion | 决定热传播，不是当前温度本身。 |

### 缺口 B：属性存储粒度

必须先决定每类属性写在哪里：

- material default：材质目录默认值，例如 density、thermal conductivity。
- normal block override：单个 macro block 的局部覆盖。
- refined micro override：单个 micro slot 的局部覆盖。
- object/part attribute：prefab part 或 object runtime 状态。
- environment summary：宏格聚合后的当前温度/湿度。

如果不先定粒度，后续会出现同一属性同时写在 material、block、environment、object 四处而没有优先级。

**Normal block override 路径已实现**（Phase 5.C，2026-05-13）：
`SceneServer.Voxel.Storage.put_attribute_for_cell(storage, macro_index_or_coord, attr_name, value)`
高层 API（C-7），按 catalog name 反查 id + value_type + min/max，对 `:solid` mode
cell 的 `NormalBlockData.attribute_set_ref` 写入；intern 后池内结构等价集合复用
旧 ref（保证 chunk_hash 在不调用该 API 时 byte-stable）。Phase 5.C 简化路径
（C-7 选项 1）要求 caller 先 `put_solid_block`：`:empty` / `:refined` cell 上
直接 raise，Phase 5.D 接 cell mode 自动转换 + refined per-MicroLayer attribute
路径。`merge_rule` 字段从 catalog 取出但本 commit 不消费（override 语义）——
五层 effective value 解析（material default / normal block override / refined
micro override / object-part / environment summary）由 Phase 5.D 落地。具体
commit hash 见 `git log --oneline | grep phase5c`。

**五层粒度优先级已实现 4 层**（Phase 5.D，2026-05-13）：
`SceneServer.Voxel.Storage.effective_attribute_at(storage, macro_index_or_coord, attr_name_or_id, opts)`
高层 API（D-5），按 D-1..D-5 推荐方案（用户 2026-05-13 approve）应用 4 层
merge_rule（L4 object-part 推到 Phase 5.D.2 或更晚）：

- **D-1** override 优先级 L3 > L2 > L1 > L5（micro > macro override > material default > environment）
- **D-2** add_delta L1 base + L2/L3/L5 delta 累加
- **D-3** `temperature_delta` / `moisture_delta` 字段 + attribute_set 双路径 sum 累加（向后兼容）
- **D-4** L4 object-part attribute 暂不接（推到 5.D.2）
- **D-5** API macro 粒度

merge_rule 实施：

| merge_rule | 实施 |
|---|---|
| `override` | L3 > L2 > L1 > L5（取最高 priority 层有值的，否则次高，最后 default） |
| `add_delta` | L1 + (L2.delta ?? 0) + (L3.delta_sum ?? 0) + (L5.delta ?? 0) |
| `max` / `min` | max / min of [L1, L2, L3, L5] 中所有有值的层 |
| `material_default` | 仅 L1（忽略其他层） |

边界：effective_value 超出 `[min_value, max_value]` clip 到边界（草案 §7 推荐
策略）；未知 attr_name / 不合法 macro_index → raise。L3 refined cell 多 layer
处理：add_delta sum 所有 layer 的 delta，max/min 取所有 layer 极值，override
取 canonical 序的 first layer with attribute（不累加）。L5 当前仅 temperature /
moisture 适用，模拟器写入 `MacroEnvironmentSummary.current_temperature` 推到
Phase 5.E / 5.F。具体 commit hash 见 `git log --oneline | grep phase5d`。

### 缺口 C：模拟器和边界传播

温湿度不是单次 edit intent 的附属字段，而是服务器规则帧的状态更新。需要 Scene 侧体素状态模拟基础设施：

- dirty cell / dirty bounds 标记。**Phase 5.E 已实现**：macro cell 粒度
  `DirtyMacroBounds` + 4 个 reason_flag bit（`0x01 attribute_write` /
  `0x02 chunk_sub_change` / `0x04 cross_chunk_boundary` /
  `0x08 catalog_changed`）；Storage 写入路径（put_solid_block /
  put_micro_block(s) / clear_micro_block / clear_macro_cell /
  put_attribute_for_cell）全部自动 mark `0x01`。
- 每 tick 或低频规则帧处理温度和湿度扩散。**Phase 5.E 已落 per-chunk
  10 Hz (100ms) tick scheduler**（嵌入 `ChunkProcess` state），由
  `SceneServer.Voxel.Simulator` behaviour 承接；具体温湿度 simulator 推到
  Phase 5.F（本 commit 框架就绪，**不注任何具体 simulator**）。
- chunk 边界事件，跨 chunk 传播时带 lease / owner_epoch fence。
  **Phase 5.E 拉模式占位**：`env.neighbor_lookup` 字段已定义，simulator
  可在 tick 内主动读邻 chunk（1 tick 滞后可接受）；上层用
  `Storage.mark_macro_dirty/3` + `reason_cross_chunk_boundary` 显式写入
  bit；具体邻区路由 / lease fence 推到 Phase 5.F / Phase 6。
- deterministic hash，用于回归验证相同输入产生相同 delta。**Phase 5.E
  已实现** `SimulationTick.output_hash(input_chunk_hash, dirty_bounds,
  tick_seq, simulator_ids)`（xxHash64）；同输入 → 同输出。
- `EnvironmentUpdated` / `CatalogPatch` delta 下发。**Phase 5.F 已实现**
  （2026-05-13）：新增 wire opcode `0x72 EnvironmentUpdated` codec
  (`Codec.encode/decode_environment_updated_payload`，wire layout 见
  `docs/2026-04-10-线协议规范.md` §0x72 段)，ChunkProcess `execute_simulation_tick`
  在 `SimulationTick.run_tick` 返回后对每个非空 `env_delta` fanout 编码后的
  payload 给本 chunk 的 subscribers，并 emit `voxel_environment_updated_push`
  / `voxel_environment_updated_skipped` observe 事件。`field_mask` 仅接受
  `0x01` temperature / `0x02` moisture / `0x03` 双字段，未知 bit 硬错误（与
  `0x71 CatalogPatch` envelope 同款）。Phase 5.F 本 commit **不修改**
  `storage.environment_summaries` 的 canonical truth（仅推 wire），writeback
  + chunk_version bump 推到 Phase 5.F.runtime。
- temperature / moisture diffusion simulator。**Phase 5.F 已实现**
  （2026-05-13）：`SceneServer.Voxel.DiffusionSimulator` 实现 `Simulator`
  behaviour，参数化 `attribute_name` (`"temperature"` / `"moisture"`) /
  `alpha` / `dt`，单一模块支持温度 / 湿度（草案 F-2 推荐方案）。算法 3D
  7-stencil 显式扩散（草案 F-1 macro 粒度）。值域复用
  `MacroEnvironmentSummary.current_temperature / current_moisture` i16 raw
  delta，simulator 直接在 i16 域做扩散。α / dt 从 simulator config 取，Phase
  5.F 不接 catalog `thermal_conductivity` 动态查询。边界拉模式邻 chunk +
  Neumann fallback（草案 F-3 推荐）。Courant 条件 α × dt × 6 < 1 数值稳定。
  `tick/3` behaviour 入口 + `tick/4` 显式 config 纯函数版本。注册策略：
  `config :scene_server, :voxel_simulators` 保持 `[]`，runtime 注册推到
  Phase 5.F.runtime 以保证 603 baseline byte-stable（草案 §step 6 硬纪律）。

Phase 5.E 实施依据 `docs/docs/20-archive/field-emergence/2026-05-13-phase5e-simulation-tick-infrastructure.md`
E-1..E-6 全部推荐方案；Phase 5.F 实施依据
`docs/docs/20-archive/field-emergence/2026-05-13-phase5f-temperature-moisture-simulator.md` F-1..F-7
全部推荐方案（用户 2026-05-13 approve）；详见
`apps/scene_server/lib/scene_server/voxel/README.md` "Phase 5.E: scene
simulation tick infrastructure" 段 + "Phase 5.F：温度 / 湿度 diffusion
simulator + EnvironmentUpdated delta (opcode 0x72)" 段。具体 commit hash 见
`git log --oneline | grep -E 'phase5e|phase5f'`。

## 不建议走的补丁路线

- 不要在 `OnlineVoxelWorldAdapter.placeMicroBlock` 里直接调用本地 `WorldStore`，这会破坏在线 confirmed truth。
- 不要把 micro slot 编进 `impact_kind` 或临时复用 material id，后续 object part、attribute patch、damage intent 都会被挤爆。
- 不要只在客户端启用 refined prefab 热栏，服务端不认识的 blueprint 会继续制造表现和权威不一致。
- 不要在 Gate 里继续扩展 prefab cell loop，跨 chunk 原子性必须由 World/Scene transaction 基础设施承载。
- 不要把密度、导热性直接塞进 `NormalBlockData` 固定字段；更适合走 attribute catalog 和 material defaults。

## 建议实施顺序

### Phase 1：权威 truth contract 收口

交付物：

- `RefinedCellData` 服务端类型、TS 类型、binary codec。
- `AttributeSet` / `TagSet` 空间和 catalog patch 的最小类型。
- snapshot / delta golden fixtures，覆盖 macro、refined、environment、attribute/tag refs。**Phase 1.6a 已实现（服务端侧）**：22 条 fixture 落在 `apps/scene_server/priv/fixtures/voxel/`，验证脚本 `test/scene_server/voxel/golden_fixture_test.exs` 32 tests 全绿，3 pinned `chunk_hash` baseline byte-stable。**Phase 1.6b 已实现（客户端侧）**：vitest 加载同一批 fixture，跑 TS decoder + chunk_hash 等值断言，新增 44 tests 0 回归（299 baseline）。
- `chunk_hash` / `cell_hash` 覆盖全部 truth section。**已实现**（Phase 1a `RefinedCellData` + 1.2 `AttributeSet` + 1.3 `TagSet` + 1.4 `CatalogPatch`）。

验收（Phase 1 全部 4 条已通过 → Phase 1 全收口）：

- Elixir codec roundtrip。**已实现**（Phase 1.6a，golden fixture 双向 byte-stable + chunk_hash 等值）。
- TS decoder roundtrip。**已实现**（Phase 1.6b，attributeSet/tagSet/catalogPatch + voxelProtocol snapshot 完整字段解码 + delta/invalidate/object_state_delta/catalog_patch dispatch 全部 byte-stable decode）。
- 相同 fixture 的服务端和客户端 hash 一致。**已实现**（Phase 1.6b，`.yaml` 中 `chunk_hash` 与客户端从 snapshot payload byte offset 40 读出的 u64 完全等值）。
- 在线 Web CLI 可读取 refined cell，但不能本地伪造写入。**既有约束**（online 模式下 `WorldStore` 只应用服务端 `ChunkSnapshot / ChunkDelta / VoxelIntentResult`，参见本文档 §"基础事实"）。

### Phase 2：服务端 refined micro edit

**状态：已实现核心（2026-05-13 verifier 审计通过 §2.2 三条验收口径）**。详见 `apps/scene_server/lib/scene_server/voxel/audit/2026-05-13-phase2-verifier-audit.md`。已落地能力：typed VoxelEditIntent v1（`apps/gate_server/lib/gate_server/codec.ex` 616-701，15 字段）+ Storage.put_micro_block / put_micro_blocks / clear_micro_block（`apps/scene_server/lib/scene_server/voxel/storage.ex` 177-324）+ CellRefined delta_kind=2 encode/decode/apply（`apps/scene_server/lib/scene_server/voxel/codec.ex` 937-986, 1282-1340 与 `chunk_process.ex` 2295-2334）。设计依据见 `docs/docs/20-archive/voxel-authority/phase-1c-refined-mutation.md`（含 Decision 2 / 4）。

Follow-up（不阻塞 Phase 3）：4 个可选 storage 函数（`merge_refined_cell` / `split_solid_macro_to_refined` / `clear_refined_cell_if_empty` / `collapse_full_refined_to_solid`）；服务端 iex-shell CLI（`micro_cell` / `micro_place` / `micro_break` —— web_client `devToolsCli.ts` 已实现客户端 CLI）。

交付物：

- Scene storage refined mutation API。
- `CellRefined` delta encode/decode/apply。
- typed edit intent 第一版。
- CLI：`micro_cell` 读取、`micro_place` / `micro_break` 提交服务端 intent。

验收：

- 左键破坏 refined micro slot 后，只移除目标 slot。
- 右键放置 refined micro slot 后，snapshot/delta 回推可读。
- 在线模式刷新后状态来自服务端持久化，不来自浏览器 local storage。

### Phase 3：prefab v2 事务化

**状态：pass-with-followup（2026-05-13 verifier 审计通过 §3.2 三条验收口径）**。详见 `apps/scene_server/lib/scene_server/voxel/audit/2026-05-13-phase3-verifier-audit.md`。已落地能力：World `TransactionCoordinator` / `TransactionExecutor` 全在线（含 Phase 3-bis :prepared resume fast path）；Scene `BuildTransactionApplier` fence + staged intent + commit atomic + Phase 3-bis `pending_fence` 持久化到 `voxel_chunk_pending_transactions`；Gate `tcp_connection.ex` `apply_voxel_prefab_place_intent` → `run_prefab_transaction` 三分支接入（`single_chunk_fast_path` / `same_owner_fast_path` / `coordinator_begin_transaction`）；observe events 三层完整 emit（gate / scene / world）；`BlueprintCatalog` v2 single-macro micro mask（3 builtin）。Audit baseline：scene 441 voxel tests + world 1 doctest + 116 tests，0 regression。

Follow-up（按 audit §6 评级，全部不阻塞 Phase 3 §3.2 AC）：跨 macro refined blueprint catalog（Phase 5 catalog 真实化）；`PrefabRaster.layer_attrs` 扩展到 attribute_set_ref / tag_set_ref / owner_object_id / owner_part_id（Phase 5 prefab raster 真实化，Phase 4 已就绪 provenance 一侧）；`PrefabRasterPatch` 结构（Phase 5）；split-owner 跨 region multi-participant 真实集成测试（A4-bis-cluster）；浏览器 / CLI / observe 三入口端到端集成测试（独立 integration sprint）。

交付物：

- 服务端 refined blueprint catalog。
- Prefab raster 输出 refined patches + object refs。
- World transaction coordinator 接入 Gate prefab path。
- prepare/commit/abort observe events。

验收：

- 跨 chunk prefab 要么全部落地，要么全部不落地。
- 中途 lease/route 失败不会留下部分 chunk 小尾巴。
- 浏览器右键 prefab、CLI prefab_place、服务端 observe 三条入口结果一致。

### Phase 4：object provenance 与局部破坏

**状态：已实现（2026-05-13 verifier 审计通过）**。详见 `apps/scene_server/lib/scene_server/voxel/audit/2026-05-13-phase4-verifier-audit.md`。已落地能力以 `apps/scene_server/lib/scene_server/voxel/README.md` Phase 4 / 4-bis / A4 / A4-4 段为权威记录。

**Phase 4 未关闭的 gap（不阻塞验收，列入 Phase 5 / A4-bis-cluster 跟踪）**：

1. damage dispatch `Task.start` 丢弃路径缺少 dropped-damage observe counter（chunk_process.ex 异步 dispatch best-effort）。
2. owner-lookup cold-start 退化 `%{owner_key => covered_chunks}`，A4-bis-cluster 加 `MapLedger.region_for_chunk` 后退役。
3. PartState health 初始值统一用 ratio=1.0，Phase 5 引入 `PartDefinition.default_health_ratio` 协议字段后改 per-part。
4. e2e 测试用 `assert_eventually` 1s deadline，偶发 CI 慢机器 transient flake（15+ 次连续通过未复现）。

交付物：

- `microOwnerObjectIds` 或等价压缩结构。
- ObjectInstance / PartRuntimeState 最小模型。
- object_at / part_at / object_damage_preview CLI。
- 左键命中 prefab part 时提交 object-part damage/break intent。

验收：

- 命中同一 macro 内不同 prefab part 能得到不同 owner object / part。
- 局部破坏只影响目标 object/part 的 slots。
- delta 中包含 geometry 变化和 object state 变化。

### Phase 5：属性目录和温湿度基础模拟

**状态：已实现（2026-05-13 Phase 5 全收口）**。Phase 5.A-5.F 全部 done，落地后
Phase 1-5 全 done，Phase 6 FieldLayer 可开工。

交付物（全部已实现）：

- AttributeCatalogSnapshot / CatalogPatch 最小实现。**Phase 5.A**（opcode
  `0x6E`）+ Phase 1.4 (`0x71`) 已实现。
- TagCatalogSnapshot envelope（**Phase 5.B**，opcode `0x6D`）。
- 5 个 typed attribute seed + AttributeCatalog / TagCatalog runtime
  GenServer + ETS（**Phase 5.C**，含 density / thermal_conductivity 等
  material defaults + temperature / humidity / moisture environment
  current field）。
- 5 层 merge_rule 实施 4 层（L4 推后续，**Phase 5.D**
  `effective_attribute_at`）。
- Scene 低频规则帧 + dirty bounds + lease/dirty/simulator fence + 拉模式邻
  chunk + deterministic output_hash（**Phase 5.E** `Simulator` behaviour +
  `SimulationTick`）。
- temperature / moisture 3D 7-stencil DiffusionSimulator + `EnvironmentUpdated`
  (opcode `0x72`) wire payload + ChunkProcess fanout（**Phase 5.F**）。

验收：

- CLI 可读 cell effective attributes。**Phase 5.D `effective_attribute_at`
  已实现**（4 层 merge_rule，L4 推后续）。
- 修改材质或环境后，服务端生成确定性 environment delta。**Phase 5.E
  `output_hash` + Phase 5.F `DiffusionSimulator` + `EnvironmentUpdated`
  delta wire 已实现**。
- 客户端只根据权威 delta 更新表现，不本地计算 confirmed 温湿度。**协议
  上 opcode 0x72 已冻结**，wire layout 一旦发出即不变；web_client TS
  decoder 推到后续（Phase 5.F 草案 F-7 推荐方案）。

具体 commit hash 见 `git log --oneline | grep -E 'phase5[a-f]'`；详细实施
依据见 `docs/plans/2026-05-13-phase5a-...` ~ `phase5f-...` 系列草案。
**Phase 1-5 全 done**。

## 回归验收口径

每个阶段完成后，都需要同时覆盖：

- 服务端测试：Scene storage / codec / transaction / DataService persistence。
- Gate 协议测试：WS 与 TCP 同语义。
- Web 单测：decoder、online adapter、CLI、world edit controller。
- 浏览器 smoke：HUD/CLI 显示 `voxel_sync=server-authoritative`，编辑结果通过 snapshot/delta/result 读回。
- observe 日志：记录 request id、actor、target、chunk list、transaction id、result code、conflict reason。

### Phase 6：局部场域最小可行（FieldLayer + 电场 + 温度场 + web_client 调试叠加层）

**状态：已实现（2026-05-13，commit c0d8681）**。Phase 6 全部 done，Phase 1-6 全收口。

交付物（全部已实现）：

- **FieldLayer**：密集 f32 binary 数组（4096 cells × 4 B = 16 KB），
  `apps/scene_server/lib/scene_server/voxel/field/field_layer.ex`。
- **FieldRegion**：AABB 边界 + 多 FieldLayer + source_points + tick_count + lease_token，
  `field_region.ex`。
- **ElectricField**：BFS/Dijkstra 从 source_points 传播，`:gb_sets` 做 max-heap
  （`{-potential, idx}` 取反），路径代价 = `1.0 / max(density / 65536.0, 0.001) × 0.1`，
  ionization 在 |potential| ≥ 50.0 时 +5，每 tick -1 衰减；`electric_field.ex`。
- **TemperatureField**：3D 7-stencil 显式扩散，`α = min(base_alpha × tc / default_tc, 0.5)`，
  `β = 0.01` 向 env_temp=20°C 衰减，source_points 每 tick 扩散后重写；`temperature_field.ex`。
- **FieldCodec**：opcode `0x73 FieldRegionSnapshot`（field_mask 稀疏格式，f32 小端）+ opcode
  `0x74 FieldRegionDestroyed`（destroy_reason enum 4 值）；`field_codec.ex`。
- **FieldTickWorker**：per-region GenServer，独立 10 Hz 调度，监控 ChunkProcess，
  chunk 死亡时 :normal 退出；`field_tick_worker.ex`。
- **FieldTickSupervisor**：`DynamicSupervisor`，`restart: :temporary`；`field_tick_supervisor.ex`。
- **ChunkProcess 集成**：`create_field_region / destroy_field_region / push_field_snapshot_payload /
  push_field_region_destroyed_payload`；lease 变更时 stop 全部 field worker + fan-out 0x74。
- **Gate forward 路径**：`tcp_connection.ex` 新增两条 `handle_info` 把 `0x73 / 0x74` payload
  直接转发给订阅客户端。
- **线协议文档**：`docs/2026-04-10-线协议规范.md` 新增 0x73 / 0x74 完整 payload 定义。
- **web_client FieldDebugOverlay**：Three.js `InstancedMesh` 双层（温度蓝→红 / 电场黑→黄）
  + AABB 线框，F8 热键切换，隐藏默认（§5.5 硬约束）；`fieldProtocol.ts` + `fieldDebugOverlay.ts`。

**测试**：服务端新增 36 个 field 单测（6 文件）→ 656 总 0 failures；web_client 新增 9 个
vitest → 352 总 0 failures；3 个 pinned chunk_hash baseline 字节稳定。

**Phase 1-6 全 done**。详见 `apps/scene_server/lib/scene_server/voxel/README.md` Phase 6 段 +
`docs/docs/20-archive/field-emergence/2026-05-13-phase6-field-layer-minimum.md`（G-1..G-8 决策点全部推荐方案）。

---

## 当前结论

**Phase 1-6 全部验收通过（2026-05-13）**。体素服务器权威化六阶段目标已达成：

- Phase 1：truth contract 收口（RefinedCellData / AttributeSet / TagSet / CatalogPatch / golden fixtures）
- Phase 2：服务端 refined micro edit（typed VoxelEditIntent + CellRefined delta）
- Phase 3：prefab v2 事务化（World coordinator + Scene applier + fence 持久化）
- Phase 4：object provenance 与局部破坏（ObjectRegistry + PartState + ObjectStateDelta）
- Phase 5：属性目录和温湿度基础模拟（AttributeCatalog + DiffusionSimulator + EnvironmentUpdated）
- Phase 6：局部场域（FieldLayer + electric/temperature field tick + FieldDebugOverlay）

后续扩展点（不阻塞主线）：Phase 5.C.2 catalog 持久化、Phase 5.F.runtime 多实例 simulator 注册、
Phase 2/3/4 各 follow-up 项，以及面向魔法系统的 SpellPlan 接入（见 TheWorldBook §最小可行融合路径）。
