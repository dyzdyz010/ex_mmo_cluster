# 2026-04-14 文档完善执行计划

## 目标

把项目当前主干从“局部有文档”推进到“运行时主干、客户端主干、demo/auth 辅助链路都能在仓库内自解释”。

## 覆盖范围

### 1. 运行时主干模块

- `apps/scene_server/lib/scene_server/**`
- `apps/gate_server/lib/gate_server/**`
- `clients/bevy_client/src/**`

### 2. demo / auth 辅助链路

- `apps/auth_server/lib/auth_server/demo/**`
- `apps/auth_server/lib/auth_server/{application,sup,worker}/**`

### 3. 目录级结构说明

- supervisor subtree
- worker subtree
- movement / combat / npc / native subtree
- client subtrees
- demo subtree

### 4. 持久化文档规范

- 将后续代码撰写时的文档要求写入仓库指令文件

## 执行分解

1. 盘点剩余未充分说明的模块与目录
2. 补齐模块级 `@moduledoc` / `//!`
3. 补齐核心公开函数 `@doc` / `///`
4. 在大目录内部补 README，说明：
   - 监督树
   - 运行时分层
   - authority / adapter / actor 关系
5. 在 `AGENTS.md` 中加入后续文档规范
6. 跑测试，确保文档改动未破坏代码

## 完成标准

- 当前主干 runtime / client / demo 模块均有清晰说明
- 主要子目录都有 README
- 仓库指令文件已经包含后续文档规范
- `cargo test` / `mix test` 通过
