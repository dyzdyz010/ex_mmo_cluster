# 当前已知缺口

> 2026-07-04 Voxia 客户端渲染补记：SVO 8km real-RHI smoke 已收紧到 `until_svo_uploaded`，不只验证 Transport 侧 artifact / runtime resource ready，还要求当前 `svo.revision` 被 `AVoxiaWorldActor` 消费并完成上传：`presentation_revision == revision`、`presentation_consumed=true`、`upload_complete=true`、`upload_queue=0`。本次 8km WorldGen SVO smoke 记录 `macro_cell_count=21016`、`quad_count=3697692`、`runtime_payload_bytes=7282436`、`seam_check.status=pass`、`raymarch_world_space=true`、`raymarch_root_lookup=true`、`raymarch_invalid_samples=0`、`rendering_ready.ready=true`。后续已补 `until_svo_composited` 与 composite metrics 回流；8km real-RHI composite smoke 记录 `raymarch_composite_scene_color_written=true`、`raymarch_composite_output=[1423,889]`、`raymarch_composite_nodes=96712`、`raymarch_composite_root_lookup=true`、`rendering_ready.raymarch.composite.ready=true`。这仍是客户端渲染里程碑证据，不代表服务端远景生产权威源、opcode/AOI 规则、持久化 artifact 或 launcher/update 已完成。
> 2026-07-04 Voxia 客户端渲染补记 2：stdio CLI 已补 `capture_screenshot` / `audit_png`，8km real-RHI overview smoke 在 composite ready 后请求无 UI 截图并做 PNG 像素审计。产物为 `clients/Voxia/Saved/voxia_svo_8km_overview.png`，尺寸 1423×889，文件 1303373 bytes，审计结果 `non_black_ratio=0.578711`、`unique_colors=51970`、`passed=true`。这把证据从“pass 写 SceneColor”推进到“真实 RHI 截图里有大范围可见地形”；剩余客户端风险收敛为 production renderer 化、移动中稳定性、profile 扩大和长期帧时间记录。
> 2026-07-04 Voxia 客户端渲染补记 3：stdio CLI 的 `until_svo_uploaded` / `until_svo_composited` 已支持可选 `min_revision`。跨 tile 8km real-RHI smoke 在 `move 12000 0 0` 后等待 revision 2，最终 `svo` 为 `center_tile=[12,0,-51]`、`quad_count=3679821`、`runtime_payload_bytes=7268420`、`presentation_revision=2`、`presentation_consumed=true`、`upload_complete=true`、`upload_queue=0`、`reused_macro_cell_count=20760`、`cache_hit_rate=0.988`、`seam_check.status=pass`、`raymarch_composite_scene_color_written=true`、`rendering_ready.raymarch.composite.ready=true`。这证明移动后的 8km SVO 复用/重上传不会卡住客户端 screen-pass 渲染链路。
> 2026-07-04 Voxia 客户端渲染补记 4：stdio CLI 已补 `render_perf` / `sample_render_perf`。8km overview perf smoke 在高空视角触发 revision 2 后等待 upload/composite 完成，再采样 10 秒；`clients/Voxia/Saved/svo_8km_perf_smoke.log` 记录 `sample_count=10`、`average_fps=69.014`、`min_fps=42.790`、`max_frame_ms=23.370`、`min_average_fps=30`、`passed=true`，最终 `render_perf.average_fps=85.843`，同时 `rendering_ready.ready=true`、`upload_complete=true`、`upload_queue=0`。剩余风险不再是“有没有结构化性能入口”，而是更大 profile、长期帧时间分布和 production renderer 化。
> 2026-07-04 Voxia 客户端渲染补记 5：为最大半径 raymarch profile 新增 `-VoxiaSvoRaymarchOnly` / `-VoxiaSvoSkipProxyMesh`，让超大远景只保留 runtime SVDAG buffer + composite pass，不再上传数百万 quad 的传统 SVO proxy mesh。请求 `-VoxiaSvoTileRadius=108` 当前会 clamp 到 radius 96（约 10.752km）；`clients/Voxia/Saved/svo_max_raymarch_only_smoke.log` 记录 `macro_cell_count=37240`、`quad_count=4779271`、`runtime_payload_bytes=9322308`、`upload_queue=0`、`raymarch_composite_scene_color_written=true`、`rendering_ready.raymarch.composite.ready=true`，`sample_render_perf` 为 `average_fps=120.807`、`min_fps=96.753`、`max_frame_ms=10.336`、`passed=true`。截图 `clients/Voxia/Saved/voxia_svo_max_raymarch_overview.png` PNG 审计 `non_black_ratio=0.875069`、`unique_colors=3390`、`passed=true`。这把当前客户端超大地形第一里程碑的主要风险从“传统 proxy mesh 显存 OOM”收敛到 production camera-correct renderer、长期帧时间分布和更完整的权威源/launcher 后续工程。
> 2026-07-05 Voxia SVO 生产化补记：客户端已接 `-VoxiaSvoSourcePages` 与 `svo_source_pages_v1` manifest gate，校验 scene/content/source/diff/material/renderer artifact version、page 存在性和 SHA1；持久化 macro-cell mesh artifact cache 已能按 source_pages 加载预物化 artifact，缺 manifest / hash mismatch / 缺 artifact 都有 `Voxia.Voxel.SvoPreview` automation 覆盖并硬诊断，不 fallback 到 WorldGen。stdio CLI `svo_source_pages_probe` 已覆盖 source_pages 正向加载、缺 manifest、hash mismatch、缺 artifact 四条路径，复跑日志为 `clients/Voxia/Saved/svo_source_pages_cli_probe_runtime_mesh_artifact.log`，`ok=true`。source_pages 生产路径现在默认 RuntimeMesh / `UDynamicMeshComponent`，判定同时绑定 `-VoxiaSvoSourcePages` 和 `SourceKind=source_pages` build result；`-VoxiaSvoRenderBackend=ProcMesh` 只作为显式调试基线；WorldGen / confirmed-store 预览路径仍默认 ProcMesh，`Voxia.Gameplay.WorldActor` automation 已覆盖该策略；CLI probe 也验证 source_pages artifact 可生成 renderable RuntimeMesh 输入（3604 vertices / 1802 triangles / 901 quads）。剩余缺口收窄为真实 launcher/update 或离线物化 source pages+artifact、服务端 bounded materialization / delta dirty 调度、RuntimeMesh source_pages real-RHI 画面审计和长期性能门槛。

> 2026-07-05 Voxia SVO 生产化补记 2：source page manifest 与 payload root 已解耦，`-VoxiaSvoSourcePageRoot=...` 显式指定时相对 page path 相对该 root 解析，错误 root 会以 `source_pages_missing` 阻断 far-field source。stdio CLI 新增保留型 `svo_source_pages_fixture [tile_x tile_y tile_z]`，可生成重启后复用的 `manifests/`、`source_pages/`、`artifacts/` 分离包。source_pages 默认 RuntimeMesh 的真实 RHI 第一片已通过：`clients/Voxia/Saved/svo_source_pages_runtime_mesh_y2_upload_smoke.log` 退出 0，最终 `source_kind=source_pages`、`renderer_backend=runtime_mesh`、`source_pages_present=1` / `missing=0`、`quad_count=958`、`presentation_consumed=true`、`upload_complete=true`、`upload_queue=0`、`seam_check.status=pass`；截图 `clients/Voxia/Saved/voxia_svo_sourcepages_runtime_mesh_y2.png` 审计 `unique_colors=6901`、`non_black_ratio=1`、`passed=true`，人工复核可见 voxel side wall。当前剩余缺口不再是单页 source_pages real-RHI 能否渲染，而是多 tile / 多 y 层真实包、launcher/offline 端到端、移动边界/focus suppression、长期性能，以及服务端生产权威源调度。
> 2026-07-05 Voxia SVO 生产化补记 3：`svo_source_pages_fixture` 已支持可选 `[radius_tiles] [vertical_radius_tiles] [near_skip_radius_tiles]`，并在 `vertical_checks` 中逐层验证同一 manifest/cache 对上下 y 层 center 切换仍可命中。当前 CLI 证据 `clients/Voxia/Saved/svo_source_pages_fixture_multiy_radius1.log` 记录 3x3x3 包 `stored_source_page_count=27`、`stored_artifact_count=27`、`vertical_checks.ok=true`、`expected_pages=9`、`artifact_loaded_macro_cells=9`。当前剩余缺口中的“多 tile / 多 y 层真实包”已从客户端 fixture 能力问题，收窄为 launcher/offline 生产包生成、真实移动边界回归和长期性能验证。
> 2026-07-05 Voxia SVO 生产化补记 4：3x3x3 source_pages 保留包的真实 RHI RuntimeMesh 上传已通过，日志为 `clients/Voxia/Saved/svo_source_pages_multiy_radius1_real_rhi_upload_worldgenpreview_retry.log`，退出 0。最终 `svo` 记录 `center_tile=[11,0,-51]`、`source_kind=source_pages`、`renderer_backend=runtime_mesh`、`source_complete=true`、`farfield_source_ready=true`、`source_pages_expected=9`、`source_pages_present=9`、`source_pages_missing=0`、`source_pages_hash_mismatch=0`、`mesh_artifact_ready=true`、`artifact_cache_ready=true`、`artifact_cache_loaded_macro_cells=9`、`quad_count=7435`、`cache_hit_rate=1.000`、`presentation_revision=2`、`presentation_consumed=true`、`upload_complete=true`、`upload_queue=0`、`seam_check.status=pass`。当前“多 tile / 多 y 层真实包”已不再是客户端上传链路缺口，剩余是 launcher/offline 生产包生成、真实移动边界/focus suppression、长期性能门槛，以及服务端生产权威源调度。
> 2026-07-05 Voxia SVO 生产化补记 5：source_pages 回归入口已从手工 smoke 收敛为可复用脚本。`until_svo_source_pages_uploaded [timeout_ms] [expected_pages] [min_revision]` 会同时检查 source kind、RuntimeMesh backend、source/page/hash/artifact readiness、presentation consumed、upload complete、upload queue 和 seam pass；`node clients/Voxia/scripts/run_svo_source_pages_fixture_smoke.js` 会先生成保留型 fixture，再用返回的 `launch_args` 重启真实 RHI 客户端。最新产物 `clients/Voxia/Saved/svo_source_pages_fixture_runner_real_rhi.log` 退出 0，记录 `expected_pages=9`、`source_pages_present=9`、`source_pages_missing=0`、`artifact_cache_loaded_macro_cells=9`、`quad_count=7435`、`presentation_revision=2`、`upload_complete=true`、`upload_queue=0`、`seam_check.status=pass`。当前剩余缺口进一步收窄为 launcher/offline 生产包生成与更新安装流程、真实移动边界/focus suppression 回归、长期性能门槛，以及服务端生产权威源调度。
> 2026-07-05 Voxia SVO 生产化补记 6：source_pages retained package 的相邻 tile 移动回归已通过。`svo_source_pages_fixture` 新增 `[movement_radius_tiles]` 后，`clients/Voxia/Saved/svo_source_pages_fixture_move_radius1.log` 记录 `movement_checks.ok=true`、`checked_centers=27`、`ready_centers=27`、`stored_source_page_count=75`、`stored_artifact_count=75`；默认 runner 会在重启上传通过后移动到 +X 相邻 tile。真实 RHI 产物 `clients/Voxia/Saved/svo_source_pages_fixture_runner_move_real_rhi.log` 退出 0，最终 `center_tile=[12,0,-51]`、`revision=4`、`source_pages_present=9`、`source_pages_missing=0`、`artifact_cache_loaded_macro_cells=9`、`quad_count=7606`、`presentation_revision=4`、`upload_complete=true`、`upload_queue=0`、`seam_check.status=pass`。当前“真实移动边界”至少已有 source_pages + RuntimeMesh 相邻 tile retained-package 证据；剩余移动类缺口收窄为更长巡航、真实 launcher/offline 包移动回归与长期性能门槛。
> 2026-07-05 Voxia SVO 生产化补记 7：source_pages + RuntimeMesh 的 focus suppression 回归已通过。`svo` / observe 新增 `suppressed_macro_cells` 与 `suppression_serial`，stdio CLI 新增 `until_svo_source_pages_suppressed [timeout_ms] [expected_pages] [min_revision] [min_suppressed]`，runner 新增 `--focus-suppress`。真实 RHI 产物 `clients/Voxia/Saved/svo_source_pages_fixture_runner_focus_suppression_real_rhi.log` 退出 0，最终 `center_tile=[11,0,-51]`、`revision=2`、`source_pages_present=9`、`source_pages_missing=0`、`artifact_cache_loaded_macro_cells=9`、`suppressed_macro_cells=9`、`suppression_serial=1`、`quad_count=7435`、`presentation_consumed=true`、`upload_complete=true`、`upload_queue=0`、`seam_check.status=pass`。这证明 retained package 的低保真 SVO far visual 会在 confirmed focus 覆盖下被裁掉；剩余 focus 类缺口是 launcher/offline 真实包、真实服务端 hydrate payload、长巡航与长期性能，而不是客户端 source_pages RuntimeMesh suppression 基本链路。
> 当前唯一事实中的“未完成 / 待迁移 / 待验证”集中列表。这里不是 backlog 全量，只记录会影响架构判断的缺口。

## 服务端控制面

- SceneNodeRegistry 仍不是完整 HA：当前有 stale owner repair，但不是容量感知 failover / 自动迁移完整方案。
- Subscription liveness 的长期正交设计仍需继续：当前已有 worker 化和多项修复，但“服务端透明维护订阅活性、客户端无感”仍是待收口方向。
- 大范围 region/materialization 的异步背压、预算和跨节点调度仍需继续工程化。

## 体素真值与基线

- **baseline 形态与流送边界决策已定（2026-06-29），实现处于全量物化过渡 → delta 边界迁移中**：目标态为确定性 WorldGen + 设计师 delta D + hash 凭证 H，storage ∝ 修改量；当前仍是 WorldPackBootstrapper/shard 装全量 chunk payload。迁移钥匙是 WorldGen 跨端 bit-exact 确定性。见 [`voxel-server-authority/2026-06-29-voxel-baseline-streaming-boundary.md`](../../../voxel-server-authority/2026-06-29-voxel-baseline-streaming-boundary.md)。
- WorldGen 噪声降级为 migration 已开始落地：`0x6A` 远景 LOD 不再重跑噪声，默认读 `LodHeightmapStore` 持久化 projection；chunk snapshot 写入可同事务 upsert projection；`LodProjection.Rebuilder` 可显式 backfill projection；`ChunkProcess` 生产默认缺 authoritative snapshot 即失败并输出 `voxel_chunk_materialization_failed`。`DefaultRegionBootstrapper` 开发/demo 可通过 `DevSeed` 写 starter chunk snapshots 并 rebuild LOD projection；WorldGen / empty chunk 仅保留为显式 dev/test materialization 辅助路径。
- 服务端真实 WorldGen world-pack 生成入口已补：`WorldPackBootstrapper` 可在启动时按显式 chunk bounds 批量写 canonical snapshots 并发布 ready manifest。仍缺 launcher/update 层的包下载、hash 校验、region manifest/index 和 diff chain 完整校验 UI/流程。
- runtime diff 的 channel/priority/budget 还未形成最终设计；当前 snapshot 仍可能成为 bulk 数据来源。
- 稀疏 chunk、完整 32km 生成预算/调度、真实地图导入 migration、launcher 包管理和完整 dirty/rebuild 调度仍未完成。LOD material 已从服务端 projection row 进入 0x6B material section，并被 Voxia decode/debug/heightmap vertex-color 表现消费；后续剩余是 catalog 驱动 palette 和完整 dirty scheduler。

## Voxia 近远景

- SVO 8km preview 已从单 section 改为 361 patch sections 分帧上传，并有 CLI / real RHI smoke 证据；VHI/SVO coverage 枚举已共用 `FVoxiaFarFieldCoveragePlanner::PlanFull`，transport async lifecycle 已共用 `FVoxiaFarFieldBuildPipeline`，patch section 上传状态机已共用 `FVoxiaFarFieldPatchUploader`，远景 ProcMesh 属性已收敛到 `FVoxiaFarFieldMeshComponentDesc`。SVO builder-side macro-cell artifact/cache/reuse 已落地，snapshot / observe 暴露 built/reused/removed/dirty/cache_hit_rate；upload-level section fingerprint 复用第一片也已落地，跨 1 tile 的 8km SVO smoke 从全量 361 patch 上传降为 39 uploaded / 322 reused。CPU SVDAG artifact 统计第一片已落地：8km SVO snapshot 暴露 `svdag_node_count=189144` / `svdag_unique_node_count=70085` / `svdag_compression_ratio=0.371`。runtime SVDAG resource 数据面第一片已落地：8km SVO snapshot 暴露 `runtime_resource_ready=true` / `runtime_root_count=21016` / `runtime_node_count=3627` / `runtime_child_ref_count=24416` / `runtime_gpu_bytes=1240896` / `runtime_compression_ratio=0.019`。2026-07-01 已补 SVO confirmed-store source 第一片：`-VoxiaSvoConfirmedSource` 从 `FVoxiaVoxelStore` 快照构建，完整覆盖时 `source_kind=confirmed_voxel_store` / `source_complete=true`，缺 coverage 时硬失败并给出 `expected_source_chunk_count` / `present_source_chunk_count` / `missing_source_chunk_count` / `build_error`，不 fallback 到 WorldGen 或空气。WorldGen preview 下已补小范围 confirmed-source preload 与预算门禁；8km confirmed source 会在超过 `-VoxiaSvoConfirmedSourceMaxChunks` 时提前拒绝，而不是批量物化数百万 chunks。服务端已补 `WorldPackSvoSourceMaterializer` 与 `scripts/world_pack_svo_source_materialize.exs` 第一片：可按同构 SVO tile/macro-cell coverage 统计 canonical `expected/present/missing`，预算内可经 `WorldPackBootstrapper`/`WorldGenMaterializer` 写 bounded canonical snapshots，并在复查仍 incomplete 时显式失败。客户端 baseline pack 的本地 release manifest shard `size_bytes` / `sha256` 校验第一片已补：`world_pack_index_v1` window load 必须先校验本地 `scene_<id>_world_pack_release_manifest.json`，缺 manifest / 缺 shard entry / size 或 hash mismatch 都阻止进入 Gate/Scene streaming；`ConnectGate` / `EnterScene` 也已在 `entry_gate_ready=false` 时硬拒绝。2026-07-02 已补最小 shader dispatch probe 与 4x4 GPU preview grid readback 证据，runtime 可用 `-VoxiaSvoRaymarchPreviewGrid=N` 打开有界调试读回；2026-07-03 起该 readback 会产出 `raymarch_visual_pixels` / `PreviewColors`，并把 raymarch metrics 纳入 `svo` snapshot，作为 UI/screen pass 前的可视像素面；同日新增 `-VoxiaSvoRaymarchScreenProbe`，可把 debug 输出扩到 viewport-derived screen buffer 并通过 `raymarch_mode=screen_probe` 观测；后续又新增 `-VoxiaSvoRaymarchComposite`，通过 `FWorldSceneViewExtension` 订阅 Tonemap 后处理 pass，并用 global pixel shader 写回 debug raymarch tint SceneColor。仍未落地：8km 生产级权威 3D 源全量覆盖/物化调度、持久化 artifact、完整 launcher/update 包下载/安装 UI 与 diff-chain 可视化流程、完整 SVO traversal composited screen-space GPU raymarch renderer。
- runtime SVDAG resource 数据面第五片已补最小 shader dispatch probe，第六片已补 preview grid 取样读回，第七片已把 readback 样本映射为可视像素，第八片已把 metrics 接入 `svo` snapshot，第九片已补 screen probe debug buffer，第十片已补 screen-pass view extension hook，第十一片已补 debug tint SceneColor 写回：第二片固定 stride payload 为 node 16 个 `uint32` / 64B、root 12 个 `uint32` / 48B；第三片 RHI ByteAddress buffer wrapper 的 8km smoke 暴露 `runtime_node_word_count=58032` / `runtime_root_word_count=252192` / `runtime_payload_bytes=1240896`，WorldActor 日志暴露 `rhi_ready=1` / `rhi_bytes=1240896`；第四片新增 `FVoxiaSvoRaymarchCS` 参数结构并由 automation 验证可从 runtime GPU buffers 生成绑定参数；第五片新增独立 `VoxiaShaders` PostConfigInit 模块和 `/VoxiaShaders` shader source mapping，真实 RHI `Voxia.Voxel.SvoPreview` automation 证明 `shader_available=1` / `parameters_ready=1` / `dispatched=1` / `groups=[1,1,1]`；第六片在真实 RHI automation 中提交 4x4 preview grid compute dispatch 并读回 64 个 `uint32`；第七片断言同一 readback 生成 16 个非空 `FColor`；第八片断言 `SnapshotJson` 暴露 `raymarch_visual_pixels` / `raymarch_readback`；第九片断言 screen probe 输出尺寸预算和 `raymarch_mode=\"screen_probe\"`，真实 RHI 小半径 smoke 记录 `raymarch_grid=[240,135]` / `raymarch_visual_pixels=32400`；第十片新增 `FVoxiaSvoRaymarchCompositeExtension`，真实 RHI smoke 记录 `VoxiaSvoRaymarchComposite: postprocess pass observed output=[1920,1080] nodes=1088 max_depth=4 alpha=0.25`；第十一片新增 `FVoxiaSvoRaymarchCompositePS` 并写回 SceneColor，真实 RHI smoke 记录 `VoxiaSvoRaymarchComposite: scene color written output=[1920,1080] nodes=1088 max_depth=4 alpha=0.25`。这里的第十一片旧缺口已由下一条第十二片取代。
- 2026-07-03 后续更正：上一条的第十一片后续已补第十二片，`VoxiaSvoRaymarch.usf` 不再用 root/node hash 做 debug tint，而是在 shader 侧用 root-atlas top-down traversal 读取 SVO root/node ByteAddress payload，按 child mask / occupancy / material id 找到首个非空 leaf；preview-grid readback 固定编码 hit/miss word，真实 RHI automation 记录 `hit_samples=12` / `miss_samples=4` / `invalid_samples=0`，真实 RHI composite smoke 记录 `VoxiaSvoRaymarchComposite: scene color written output=[1920,1080] nodes=1088 max_depth=4 alpha=0.25`。剩余 gap 改为 production camera-correct world-space renderer、runtime mesh / HISM / Nanite-ready artifact，以及 launcher/update、8km 生产权威源和持久化 artifact。
- 2026-07-03 后续更正：runtime mesh / HISM / Nanite-ready artifact 已补客户端契约第一片。`FVoxiaFarFieldRenderArtifactDesc` 会记录 live FarField patch 的 fingerprint、vertex/index/quad 计数、bounds、normal/color/UV 输入就绪、collision 标记、当前 render backend 与 static / Nanite bake ready 标记；`FVoxiaFarFieldPatchUploader` 负责在上传成功后生成 artifact、删除 patch 时移除 artifact，并在 fingerprint/backend/collision 都一致时复用 live patch。验证为 `Build.bat VoxiaEditor Win64 Development ... -NoLiveCoding` 退出 0；`Automation RunTests Voxia.Voxel.FarFieldPatchUploader` 退出 0；`Automation RunTests Voxia.Voxel.FarField` 找到 5 个测试并全部 `Result={Success}`。当时剩余 gap 是真实 RuntimeMesh / HISM / Nanite 渲染后端接入；HISM 与 RuntimeMesh 后端已在后续两条补齐，Nanite bake 仍未接入。
- 2026-07-03 后续更正：SVO composite scene-depth occlusion 第一片已补。`FVoxiaSvoRaymarchCompositePS` 绑定 `SceneTextures`，默认启用 depth occlusion，并暴露 `SvoNearDeviceDepth` / `SvoFarDeviceDepth` / `DepthOcclusionBias` 参数；`-VoxiaSvoRaymarchNoDepthOcclusion` 可关闭该 debug 遮挡路径。验证为 `Build.bat VoxiaEditor Win64 Development ... -NoLiveCoding` 退出 0；`Automation RunTests Voxia.Voxel.SvoPreview` 真实 RHI 退出 0 且记录 `hit_samples=12` / `miss_samples=4` / `invalid_samples=0`；`Automation RunTests Voxia.Gameplay.WorldActor` null RHI 退出 0；真实 RHI smoke `clients/Voxia/Saved/svo_raymarch_depth_occlusion_composite.log` 记录 `depth_occlusion=1` 与 `scene color written`。剩余 gap 仍是 production camera-correct world-space SVO renderer，而不是这个 debug depth-occlusion 参数面。
- 2026-07-03 后续更正：SVO composite world-space root selection 与 root lookup DDA 第一片已补。`-VoxiaSvoRaymarchWorldSpace` 会让 composite pass 上传 view origin / axes / FOV 的 SVO macro-space 参数；runtime payload 为规则 coverage 生成 root lookup grid 并上传 `VoxiaSvoRuntimeRootLookup` ByteAddress buffer；shader 侧优先按 camera ray 做 XZ DDA 查 root，命中 root 后复用现有 top-down leaf traversal；`-VoxiaSvoRaymarchNoRootLookup` 可回退旧的 `-VoxiaSvoRaymarchWorldRootScanLimit=N` 线性扫描。验证入口为 `Voxia.Gameplay.WorldActor` 配置解析 / stats 断言和真实 RHI `Voxia.Voxel.SvoPreview` payload/RHI lookup / shader compile / preview-grid dispatch-readback 断言；真实 RHI smoke `clients/Voxia/Saved/svo_raymarch_root_lookup_composite.log` 记录 `scene color written output=[1707,1067] nodes=589 max_depth=4 ... world_space=1 root_lookup=1 root_lookup_cells=9 root_scan_limit=512`。剩余 gap 仍是 production renderer 化，而不是该 debug-gated root-lookup 第一片。
- 2026-07-04 后续更正：SVO world-space raymarch preview-grid 读回可观测性已补。`FVoxiaSvoRaymarchCS::DispatchPreviewGridProbe` 新增 world-space/root-lookup probe config，并统计 GPU readback 的 hit/miss/invalid traversal samples；`svo` / `snapshot` 暴露 `raymarch_world_space`、`raymarch_root_lookup`、`raymarch_root_lookup_grid`、`raymarch_hit_samples`、`raymarch_miss_samples`、`raymarch_invalid_samples`。`AVoxiaWorldActor` 在 `-VoxiaSvoRaymarchWorldSpace` + preview-grid/screen-probe 下会使用同一套 camera-ray 参数。验证为 VoxiaEditor Development build 退出 0；真实 RHI `Automation RunTests Voxia.Voxel.SvoPreview` 退出 0，日志记录默认 atlas `hit_samples=12` / `miss_samples=4` / `invalid_samples=0`，world-space + root lookup `hit_samples=16` / `miss_samples=0` / `invalid_samples=0` / `root_lookup=[5,5]`。stdio CLI 已补显式真实 RHI/offscreen 启动：`--real-rhi` 去掉 `-nullrhi` 并加 `-RenderOffscreen`，默认 `--nullrhi` 不变；真实 RHI world-space CLI smoke 记录 `raymarch_readback=true` / `raymarch_visual_pixels=16` / `raymarch_world_space=true` / `raymarch_root_lookup=true` / `raymarch_hit_samples=16` / `raymarch_invalid_samples=0`。剩余 gap 仍是 production renderer 化、持久化 artifact、launcher/update 和 8km 生产权威源，不是该 debug-gated readback 可观测面。
- 2026-07-04 后续更正：客户端 readiness 汇总已补渲染口径。CLI `client_network_ready` 新增 `rendering_ready`，聚合 authority scene、SVO visual artifact 和 raymarch probe 状态；非 SVO 地图不把未启用 SVO 当失败，SVO 启用时要求 revision / runtime resource / macro-cell / quad / seam pass。该字段让“联网 ready”汇总可以同时暴露客户端渲染里程碑状态，但仍不代表服务端 opcode、AOI 规则或 authority result 已完成。
- 2026-07-03 后续更正：SVO HISM 渲染后端第一片已补。新增 `FVoxiaFarFieldHismInstanceBuilder`，把 patch mesh quad 转成 HISM plane instance；`-VoxiaSvoRenderBackend=HISM` 下 `AVoxiaWorldActor` 用 `UHierarchicalInstancedStaticMeshComponent` 上传 SVO patch instances，并让 artifact 记录 HISM backend。验证为 `Build.bat VoxiaEditor Win64 Development ... -NoLiveCoding` 退出 0；`Automation RunTests Voxia.Voxel.FarFieldPatchUploader` 退出 0；`Automation RunTests Voxia.Gameplay.WorldActor` 退出 0；真实 RHI smoke `clients/Voxia/Saved/svo_hism_backend_smoke.log` 记录 `render_backend=hism` 与 `hism_instances=905`。
- 2026-07-03 后续更正：SVO RuntimeMesh / DynamicMesh 渲染后端第一片已补。新增 `FVoxiaFarFieldDynamicMeshBuilder`，把 live SVO patch mesh 合并为 `FDynamicMesh3`；`-VoxiaSvoRenderBackend=RuntimeMesh`（别名 `DynamicMesh`）下 `AVoxiaWorldActor` 用 UE 内置 `UDynamicMeshComponent` 提交 SVO 远景 visual mesh，并让 artifact 记录 RuntimeMesh backend。验证先按 TDD 让 build 因缺 `FarField/VoxiaFarFieldDynamicMesh.h` 失败；实现后 `Build.bat VoxiaEditor Win64 Development ... -NoLiveCoding` 退出 0；`Automation RunTests Voxia.Voxel.FarFieldPatchUploader` 退出 0；`Automation RunTests Voxia.Gameplay.WorldActor` 退出 0；`Automation RunTests Voxia.Voxel.FarField` 找到 5 个测试并全部 `Result={Success}`；真实 RHI smoke `clients/Voxia/Saved/svo_runtime_mesh_backend_smoke.log` 记录 `render_backend=runtime_mesh`、`runtime_mesh_vertices=3620`、`runtime_mesh_triangles=1810`。该 RuntimeMesh 是客户端 runtime visual 后端，不等于 Nanite bake、runtime distance field 或 Lumen 生产路径。剩余 gap 缩小为 Nanite 渲染/烘焙后端、production camera-correct world-space SVO renderer，以及 launcher/update、8km 生产权威源和持久化 artifact。
- 2026-07-03 后续更正：客户端 `UVoxiaClientInterestSubsystem` 第一片已落地，先把 near/far/focus 关注区域架构立住。`near` 表示高保真交互 AOI，`far` 表示低频 visual map sync（当前从 SVO/VHI/heightmap 读 Transport revision），`focus` 表示望远镜、超远程魔法等按需 hydrate 的远程高保真区域；CLI 已新增 `interest`、`interest_focus`、`interest_focus_ready`、`interest_focus_release`、`interest_remote_focus`、`interest_remote_focus_payloads`、`interest_drain_requests`、`interest_flush`、`interest_outbox`、`interest_wire_status`，`snapshot` 也会带 `interest` 字段。outbound focus 请求现在可以从 Interest runtime 转入 Transport interest outbox；默认 flush 为 `wire_disabled` dry-run，传 `wire` 在进场前会显式返回 `cannot send interest focus request before entering scene`，进场后当前 sender 会显式返回 `interest_focus_wire_opcode_unassigned`，两种情况都会保留 pending。`FClientInterestWireEncoder` 已补协议边界适配层：当前服务端 `GateServer.Codec` 没有 focus hydrate request opcode，且不能复用 `ChunkSubscribe 0x60`，因为它会改变 active/editable window 语义。receive-side 已新增 `FClientInterestHydrateSnapshot` 与 `UVoxiaTransportSubsystem::ApplyInterestFocusHydrateSnapshot`，`interest_remote_focus` 现在走 Transport receive adapter，且只有 `confirmed_truth_applied=true` 的 hydrate snapshot 才能让 focus 进入 interactive；Transport 成功应用 hydrate 后会按 `request_id` 或 `region_id` ACK pending outbox，并在 `interest_outbox` 暴露 `total_acknowledged`。`ApplyInterestFocusHydratePayloads` 已补未来 decoder 适配点：先消费 confirmed `ChunkSnapshot 0x62` / `ObjectStateDelta 0x6C` / `FieldRegionSnapshot 0x73` payload，再打开 focus interactive；`ChunkDelta 0x63` 暂不作为 hydrate seed，避免本地缺 base version 时假确认。真实 wire opcode / 服务端协议仍未接入；confirmed voxel truth 仍只由 Transport 应用服务端 authoritative frames。
- 2026-07-03 后续更正：Interest Runtime 自动刷新已补。`UVoxiaClientInterestSubsystem::Tick` 会自维护 Transport 派生的 `near` / `far` visual 状态和 focus lease/cooling，不再依赖 CLI 调用才更新；快照暴露 `auto_refresh`、`last_refresh_seconds`、`last_refresh_age_seconds`，CLI 新增无副作用观察入口 `interest_raw`。剩余缺口仍是服务端协议分配 focus hydrate request opcode、真实 decoder 接到 `ApplyInterestFocusHydratePayloads`，以及服务端租约/权限/长程命中判定。
- 2026-07-03 后续更正：真实用户 focus 请求入口已补。`T` 键和 CLI `focus_input` 现在都走 `VoxiaPawn::RequestFocusAtCrosshair`，由 `VoxiaFocusTarget` 解析 camera ray 到远程 tile，再调用 Interest promote 并转入 Transport interest outbox；`snapshot.last_focus` 暴露请求结果和 flush report。该路径已 network-ready 到客户端 outbox/feature-gated wire 边界，但服务端 focus opcode、hydrate decoder、权限/租约/长程命中规则仍未落地。
- 2026-07-03 后续更正：Transport interest outbox 自动 flush / retry 已补。pending focus 请求不再只依赖按键瞬间或 CLI 手动 flush；`Pump` 会按 `-VoxiaInterestAutoFlushSeconds` 节流重试。默认 wire disabled 时只 dry-run 保留 pending；`-VoxiaInterestWire` 会尝试 feature-gated sender，进场前返回 `cannot send interest focus request before entering scene`，进场后在服务端 opcode 未分配前仍返回 `interest_focus_wire_opcode_unassigned` 并保留 pending。剩余缺口仍是服务端协议/decoder/authority 规则，而不是客户端 outbox 生命周期。
- 2026-07-04 后续更正：Interest runtime 到 Transport outbox 的自动 drain 已补。`UVoxiaTransportSubsystem::Pump` 会刷新 Interest 并把 focus / far visual sync pending request 自动搬进 Transport interest outbox，再执行既有 auto-flush；Pawn tick、Transport tick 与 stdio CLI tick 都走同一条路径。CLI smoke 已验证 `interest_focus` 后无需手动 `interest_drain_requests`，`interest_raw.pending_outbound=0`、`auto_drain.total_drained=1`、`interest_outbox.pending=1`。剩余缺口仍是服务端正式 opcode、远景 payload 生成、focus hydrate/result 和 authority 规则，而不是客户端请求入队链路。
- 2026-07-04 后续更正：remote action 的真实用户/CLI 出站边界和客户端 wire contract 已补。`R` 键和 CLI `remote_action_input` 走 `VoxiaPawn::PerformRemoteActionAtCrosshair`，先用 `VoxiaRemoteInteraction` 检查 near/focus confirmed interactive coverage；CLI `remote_action` 在 ready 后会创建 `FVoxiaRemoteActionIntent` 并进入 Transport remote action outbox，`remote_action_outbox` / `remote_action_flush [wire]` 可观察和推进该队列，`snapshot.last_remote_action` 暴露 ready、intent id、coverage result 和 flush report。新增 `FVoxiaRemoteActionWireEncoder/Decoder`，`-VoxiaRemoteActionRequestOpcode=0xNN` 配置后可编码 `remote_action_request_body_v1` framed packet；`-VoxiaRemoteActionResultOpcode=0xNN` 配置后 inbound pump 会识别 `remote_action_result_body_v1`，`remote_action_apply_result` 可走同一 Transport adapter 并更新 `transport.remote_action_result`。剩余缺口仍是服务端正式分配 action opcode、技能 authority、租约/权限/长程命中规则，以及服务端生成对应 result frame；confirmed voxel/object/field truth 仍只走权威 snapshot/delta。
- 2026-07-04 后续更正：remote-action/focus HUD overlay 已补。默认 UMG HUD 和 `-VoxiaDebugCanvasHUD` 都会显示 action pending/sent/ack/result 计数、wire readiness、last result intent/cov/result revision、focus interactive/active/pending，以及 voxel/heightmap/VHI/SVO revision。该项把客户端 operator 可观测面补齐；剩余缺口不再是 HUD，而是服务端 opcode/authority/e2e 对接。
- 2026-07-04 后续更正：remote action result ACK 闭环已补。Transport 成功应用 `remote_action_result_body_v1` 后，会 ACK 并移除匹配的本地 pending intent；`remote_action_outbox` 暴露 `total_acknowledged`，HUD 显示 ack 计数，CLI 新增 `remote_action_result` 并让 `remote_action_apply_result` 返回 outbox snapshot。客户端 intent 生命周期已能从 pending 推进到 result ACK；剩余缺口仍是服务端正式 opcode、技能 authority、租约/权限/命中和端到端结果帧。
- 2026-07-04 后续更正：remote action intent timeout 清理已补。`FVoxiaRemoteActionIntent` 带 `timeout_seconds` / `expires_at`，Transport enqueue 默认读取 `-VoxiaRemoteActionTimeoutSeconds=N`；manual / wire flush 前会 prune expired intent，返回 `expired_before_flush`，`remote_action_outbox.total_expired` 和 HUD `exp` 计数可观测。CLI 新增 `remote_action_lifecycle_probe`，覆盖 timeout/expires、expired prune 与 result ACK。剩余缺口仍是服务端正式 opcode、技能 authority、租约/权限/命中和端到端 result frame；客户端 timeout 不是服务端拒绝。
- 2026-07-04 后续更正：remote action outbox 自动 flush / retry 已补。`UVoxiaTransportSubsystem::Pump` 会按 `-VoxiaRemoteActionAutoFlushSeconds=N` 节流重试 pending remote action intent；默认 wire disabled 时只 dry-run 保留 pending，`-VoxiaRemoteActionWire` 才走 feature-gated sender。CLI 新增 `remote_action_auto_flush`，smoke 已验证 ready focus 覆盖下生成的 intent 不用手动 `remote_action_flush` 也会进入 auto retry 观测面。剩余缺口仍是服务端正式 opcode、技能 authority、租约/权限/命中和端到端 result frame。
- 2026-07-04 后续更正：客户端联网 ready 自动化已补。新增 `Voxia.Net.ClientNetworkReadiness` automation，覆盖 focus request auto-drain、Transport interest auto-flush retry report、remote action auto-flush retry report，并验证 wire disabled / unavailable 时 pending 仍保留可观测。剩余缺口继续收敛为服务端正式 opcode、focus/far payload 生成、权限/租约/命中和 authoritative result，而不是客户端请求/重试生命周期。
- 2026-07-04 后续更正：客户端联网 ready 汇总 CLI 已补。`client_network_ready` 聚合 terrain baseline entry gate、Interest runtime/outbox/auto-flush、remote-action outbox/auto-flush/result 和 wire status，并显式输出 `requires_server_contract=true` 与剩余服务端契约列表；CLI smoke 已验证默认启动下客户端 runtime present、baseline 未 ready、opcode unassigned 都能一次性读出。剩余缺口仍是服务端正式协议和 authority/result 规则，而不是客户端观测入口。
- 2026-07-04 后续更正：远程实体 AOI 客户端可观测面已补。Transport 现在把 `PlayerEnter` / `PlayerMove 0x83` / `PlayerLeave` / `ActorIdentity` 交给 `FVoxiaRemoteActorStore` 并消费成 `remote_actors` 快照，CLI 与 `client_network_ready` 都能读到 count、enter/move/leave totals、cid、position、velocity、server tick、last seen age、update count 和 last kind；断线与成功进场会清空 live view。`Voxia.Net.RemoteActorStore` automation 已覆盖 enter/move/identity/leave/reset 生命周期。剩余缺口仍是服务端远程实体 AOI 规则、兴趣分发和实体表现层，不是客户端诊断入口。
- 2026-07-04 后续更正：远程实体入站到场景表现的客户端 smoke 已补。Transport 新增 debug-only `DebugApplyServerPayload`，CLI `remote_actor_apply` / `remote_actor_loopback` 会构造 `PlayerEnter` / `PlayerMove` / `PlayerLeave` / `ActorIdentity` 服务端 payload，并经 `DecodeServerPayloadSummary` 与 Transport 入站 apply 更新 `remote_actors`、`authority_presentation`、`authority_scene`。`Voxia.Net.RemoteActorInbound` automation 覆盖 decoder -> Transport apply -> store 路径；这仍是自动化注入面，不改变在线 confirmed state 只吃真实服务端帧的边界。
- 2026-07-04 后续更正：对象状态客户端留存已补。Transport 现在把服务端 `ObjectStateDelta 0x6C` 写入 `FVoxiaObjectStateStore`，CLI `object_state`、`transport.object_state` 和 `client_network_ready.object_state` 都能读到 object count、total updates、total affected chunks、logical scene id、object id/version、state flags、affected chunks、last seen age 和 per-object update count；初始化、断线、重新请求 terrain baseline、成功进场都会清空缓存。`interest_remote_focus_payloads` / `interest_focus_loopback` 现在可附带 0x6C debug payload，走真实 Transport hydrate adapter 后更新 `object_state`。`object_state_apply` / `object_state_loopback` 可直接构造 0x6C server payload 并走 `DebugApplyServerPayload`，用于验证对象状态入站到 `authority_presentation` / `authority_scene` 的客户端闭环；`Voxia.Net.ObjectStateInbound` automation 覆盖 decoder -> Transport apply -> store 路径，`Voxia.Net.ObjectStateStore` automation 覆盖 apply/update/reset 生命周期。剩余缺口是属性/tag patch body 的服务端 wire layout，以及服务端对象 AOI/兴趣分发规则。
- 2026-07-04 后续更正：客户端表现读模型已补。`VoxiaAuthorityPresentation` 从 Transport confirmed stores 构造 Gameplay 侧 presentation snapshot，CLI `authority_presentation` / `presentation` 与 `client_network_ready.authority_presentation` 暴露 actor/object counts、stale counts、actor speed 和对象 `normal` / `damaged` / `part_destroyed` / `destroyed` visual state。`Voxia.Gameplay.AuthorityPresentation` automation 覆盖 actor stale、object stale、flag 优先级和 JSON 输出。剩余缺口是把该读模型接到真实远程 actor/object render actor，而不是客户端状态/观测入口。
- 2026-07-04 后续更正：presentation 场景宿主已补。`VoxiaClientGameMode` 会生成 `VoxiaAuthorityPresentationActor`，该 actor 从 Transport confirmed stores 刷新 presentation snapshot，并以 debug sphere/box 表达远程 actor 与 object state；CLI `authority_scene` 与 `client_network_ready.authority_scene` 暴露 enabled/draw/rendered counts 和嵌套 presentation snapshot。`Voxia.Gameplay.AuthorityPresentationActor` automation 覆盖 chunk 坐标映射和颜色规则。剩余缺口是把 debug marker 升级为正式角色/对象资产表现、材质与生命周期反馈；当前里程碑只要求 static proxy / HISM 渲染正确。
- 2026-07-04 后续更正：authority scene 组件化 proxy 第一片已补。`VoxiaAuthorityPresentationActor` 现在默认用 sphere/cube 组件承载远程 actor 与 object-state scene proxy，同时保留 debug draw 诊断；CLI `authority_scene` / `client_network_ready.authority_scene` 暴露 `component_proxy_enabled`、component actor/object count 与 mesh readiness。`-VoxiaNoAuthorityPresentationComponents` 可关闭组件代理。该层仍只读 Transport confirmed stores，不参与碰撞、raycast、编辑或 confirmed truth。
- 2026-07-04 后续更正：authority scene proxy 生命周期第一片已补。`VoxiaAuthorityPresentationActor` 不再每帧清空重建组件，而是按 remote actor `cid` / object id 维护 proxy component map，暴露 actor/object proxy created / updated / removed totals；remote actor proxy 会向服务端确认位置平滑跟随，`interpolating_actor_proxy_count`、`actor_proxy_smoothing_seconds`、`actor_proxy_snap_distance_cm` 可从 `authority_scene` 直接观察。剩余缺口是正式角色/对象资产、材质资产绑定和大规模 proxy backend，而不是基础生命周期；当前里程碑只接 static proxy / HISM。
- 2026-07-04 后续更正：authority scene proxy 视觉状态数据通道与可见材质第一片已补。组件 proxy 现在把 actor/object 的 confirmed presentation color、state code、proxy kind 和 custom-depth stencil 写入 component custom primitive data / stencil，同时绑定 `M_VoxelVertexColor` 并用 per-component vertex color override 把状态色真正显示到 sphere/cube/HISM proxy 上；`authority_scene` 暴露 `component_custom_primitive_data_enabled`、`proxy_visible_material_ready`、`proxy_vertex_color_applied_total`、`proxy_vertex_color_failed_total`、`actor_proxy_visual_updated_total`、`object_proxy_visual_updated_total`、object visual-state counts、`last_object_proxy_visual_state`、`last_object_proxy_color` 和 `last_object_proxy_stencil`。`Voxia.Gameplay.AuthorityPresentationActor` automation 覆盖颜色编码、state code、stencil 规则和 proxy 材质资产加载。剩余缺口是最终角色/对象资产替换，以及正式材质对 custom primitive data 的生产化细化；当前仍用 static proxy / HISM 验证渲染正确性。
- 2026-07-04 后续更正：authority scene proxy 生命周期视觉反馈第一片已补。remote actor 与 object-state proxy 在创建或收到权威更新时会触发短 scale pulse，`-VoxiaAuthorityProxyPulseSeconds` / `-VoxiaAuthorityProxyPulseScale` 可调，`-VoxiaNoAuthorityProxyPulse` 可关闭；component 后端逐帧更新 transform，object HISM 后端会在 pulse 活跃且时间推进时重建 instance transform。`authority_scene` 暴露 `proxy_pulse_seconds`、`proxy_pulse_scale`、`actor_proxy_pulsing_count`、`object_proxy_pulsing_count`、`actor_proxy_pulse_started_total`、`object_proxy_pulse_started_total`。剩余缺口收窄为最终角色/对象资产和正式特效接入，不是 proxy 生命周期反馈；当前里程碑保持 static proxy / HISM。
- 2026-07-04 后续更正：authority object proxy HISM 后端第一片已补。`-VoxiaAuthorityObjectProxyBackend=HISM` / `-VoxiaAuthorityObjectProxyHism` 会让 `VoxiaAuthorityPresentationActor` 按对象视觉状态/Stencil 分组提交 `UHierarchicalInstancedStaticMeshComponent` instance，并在 `authority_scene` 暴露 `object_proxy_backend`、`object_proxy_hism_enabled`、`object_hism_component_count`、`object_hism_instance_count`、`object_hism_rebuild_total` 和 `object_hism_submitted_total`。验证为 VoxiaEditor Development build 退出 0；`Automation RunTests Voxia.Gameplay.AuthorityPresentationActor` 退出 0；CLI `object_state_loopback ...; authority_scene` 在 `-VoxiaAuthorityObjectProxyBackend=HISM` 下记录 `object_proxy_backend="hism"`、`object_hism_component_count=1`、`object_hism_instance_count=1`。该后端只解决对象状态 proxy 的批量渲染提交，不替代 SVO；SVO far-field 仍走独立的 `-VoxiaSvoRenderBackend=...` 与 raymarch 路线。剩余缺口是正式资产/材质/特效接入和生产规模调参。
- 2026-07-03 后续更正：Interest Sync Scheduler 已补。`interest` 快照内新增 `sync_scheduler`，CLI 新增 `interest_sync`；它把 near / far / focus 的 `sync_hz` 解释为可观测调度状态，暴露 priority、due/stale、budget deferral、focus suppress 和本轮 action。默认 near / focus 高优先级，far 低频 visual map sync 低优先级；被 focus 覆盖的 far 不抢同步预算，长期未服务会显式 stale。该 scheduler 只表达客户端请求策略，不拥有 confirmed truth，也不绕过 Transport outbox 或服务端协议。
- 2026-07-04 后续更正：far visual map sync 出站生命周期已补。scheduler 到期时会为未被 focus 压制的 far region 创建 `far_visual_sync` request，带 `backend` / `sync_hz` / `known_revision` / `expires_at`，并固定 visual-only（`requires_confirmed_snapshot=false`）；Transport interest outbox 会合并同 region 同 kind pending 请求，`interest_wire_status` 暴露 `far_visual_sync` 槽，`-VoxiaInterestFarSyncOpcode=0xNN` 可编码 `far_visual_sync_body_v1`。剩余缺口仍是服务端正式分配 far sync opcode、服务端生成低频 visual map data payload，以及端到端 result / projection 更新策略；客户端该请求不会提升为 interactive truth。
- 2026-07-04 后续更正：far visual map sync result 接收侧第一片已补。`-VoxiaInterestFarSyncResultOpcode=0xNN` 配置后，客户端可解 `far_visual_sync_result_body_v1`，只接受 visual-only `0x6B` heightmap payload，应用后更新 LOD heightmap state 并 ACK 匹配 outbox 请求；CLI `interest_far_sync_loopback` 覆盖该客户端闭环。剩余缺口仍是服务端正式分配 request/result opcode、生成真实远景 visual payload，以及后续 SVO/其他远景 payload 的生产协议；客户端不会把该 result 当 confirmed chunk truth。
- 2026-07-03 后续更正：focus hydrate 接收侧 wire 边界已补。`FClientInterestWireDecoder` 提供 `TryDecodeFocusHydrateFrame` 与 `DecodeFocusHydrateBody`；当前因服务端 opcode 未分配，frame decoder 不会识别任何现网帧，避免误吃 `ChunkSnapshot 0x62` / `ChunkDelta 0x63` 等既有协议。`focus_hydrate_body_v1` body contract 已可通过 CLI `interest_decode_focus_hydrate_body` 和自动化验证；Transport inbound pump 已预留 `ApplyInboundInterest`，未来 opcode 分配后可直接把 decoded confirmed payloads 交给 `ApplyInterestFocusHydratePayloads`。
- 2026-07-03 后续更正：focus 协议槽运行时配置已补。客户端支持 `-VoxiaInterestFocusPromoteOpcode=0xNN` / `-VoxiaInterestFocusHydrateOpcode=0xNN`，`interest_wire_status` 暴露 assigned/valid/opcode/reason。默认仍 unassigned；配置到既有协议 opcode（如 `0x60` / `0x62` / `0x63`）或 promote/hydrate 相同会 `valid=false` 并拒绝编码/识别。合法配置后，focus promote 会编码 `focus_promote_body_v1` framed packet，focus hydrate frame 会按 `focus_hydrate_body_v1` 解码并进入 Transport confirmed payload adapter。剩余缺口仍是服务端正式分配 opcode 和实现 authority 规则。
- 2026-07-03 后续更正：配置 opcode 的 focus hydrate frame apply smoke 已补。Transport 新增 `ApplyInterestFocusHydrateFrame`，CLI 新增 `interest_apply_focus_hydrate_frame`；配置 `-VoxiaInterestFocusHydrateOpcode=0xNN` 后，该入口会构造完整 focus hydrate frame payload，走 wire decoder，再消费 confirmed `ChunkSnapshot 0x62` payload，ACK 匹配 outbox，并把 focus 推到 interactive。socket pump 也走同一个 `ApplyInboundInterest` / `ApplyInterestFocusHydrateFrame` adapter。
- 2026-07-03 后续更正：客户端 focus wire loopback smoke 已补。CLI 新增 `interest_focus_loopback`，在配置 promote/hydrate opcode 后可一条命令完成 focus request -> Transport outbox -> configured promote encode -> configured hydrate frame -> confirmed payload apply -> focus interactive -> outbox ACK，并返回 interest/outbox/transport 快照。该命令是客户端侧联网 ready 回归入口，不替代服务端 authority 规则。
- 2026-07-03 后续更正：客户端高保真覆盖查询已补。Interest runtime 新增 tile/chunk coverage API 与 CLI `interest_query_tile` / `interest_query_chunk`，返回当前覆盖来自 near、far visual 还是 focus，并显式标注 `interactive` / `visual_only` / `confirmed_truth_applied`。Pawn 的编辑 gating 已复用该契约：confirmed near 与 confirmed focus interactive 可作为高保真交互覆盖，far visual 与 hydrating focus 只表达可见/请求状态，不会放行 intent。服务端长程权限、技能命中、租约和正式 focus opcode 仍是未完成项。
- 2026-07-03 后续更正：客户端 active focus budget 已补。Interest runtime 默认最多保留 4 个 hydrating / interactive focus，`-VoxiaInterestMaxActiveFocus=N` 可调；超额时最旧 active focus 进入 cooling，移除本地待发送 focus request，并在 snapshot / observe 暴露 `active_focus_count`、`max_active_focus`、`focus_eviction_count` 和最近驱逐 region。该预算不删除服务端 confirmed truth，只约束客户端关注区表达与 far visual suppression。
- 2026-07-04 后续更正：客户端 focus request 过期清理已补。`FClientInterestNetworkRequest` 带 `created_at` / `expires_at`；runtime focus lease 过期时会让区域进入 cooling、移除同 region 本地 pending request，并暴露 `expired_focus_request_count` / `last_expired_focus_request_region`。Transport interest outbox 会在 `interest_flush` / auto-flush 前剪枝已过期请求，`interest_outbox.total_expired` 与 flush `expired_before_flush` 可观测。CLI 新增 `interest_lifecycle_probe [sync_budget] [max_focus]`，覆盖远景低频 sync、预算延后/stale、focus suppression、budget eviction、lease expiry 和 outbox prune。剩余缺口仍是服务端正式 focus opcode、服务端租约/权限/命中规则和端到端 hydrate/result 生成。
- 2026-07-03 后续更正：远程交互客户端 facade 已补。`VoxiaRemoteInteraction` 统一远程 action gate：confirmed near / confirmed focus 才返回 ready；far visual、hydrating focus 或无覆盖都会给出显式 reason，`remote_action` CLI 可在未 ready 时发起 focus hydrate request，`remote_action_check` 只做无副作用检查。`VoxiaPawn` 的 `T` focus 输入已复用该 facade。剩余缺口仍是服务端正式 focus opcode、技能权限、长程命中和租约规则。
- 2026-07-03 后续更正：heightmap LOD / VHI / SVO far visual proxy suppression 第一片已补。`VoxiaWorldActor` 会在 interactive focus 覆盖签名变化时重新评估远景 patch；heightmap LOD 过滤被 focus 覆盖的 quad，VHI 过滤被 focus 覆盖的 tile，SVO proxy 过滤被 focus 覆盖的 macro-cell artifact，并在日志暴露 `suppressed_quads` / `suppressed_tiles` / `suppressed_macro_cells` / `suppression_serial`。该实现不改变 confirmed store、碰撞、raycast 或服务端 truth；SVO raymarch debug resource 和最终 production renderer 的更细粒度裁剪仍未完成。
- 远程 detail-on-demand / 服务端长程 raycast / 远程实体和对象 AOI 仍是设计方向；客户端侧已有 focus 状态机、远程 actor store、object state store、authority presentation read model、presentation scene host 和可观测面，服务端协议、租约、权限、长程命中判定和 AOI 分发规则仍未落地。

## 局部场 / 涌现

- Generic persistent FieldSource owner 存活探测、预算消耗、自动续租、跨 chunk lifecycle 未完成。
- FieldEffect dispatcher 仍需 batch mutation，避免单 tick 多次 version bump / fan-out / persist enqueue。
- Phase 8 effect 边界未落地：ignite/freeze/melt/damage/object/combat/source effect 仍需统一 dispatcher。
- 完整电路仿真、材料熔断破坏、tick-by-tick 能量扣减未完成。
- SurfaceElement 物理参与、客户端完整渲染/解码、delta 专用 op 未完成。
- Prefab/object 统一 field participant projection 尚未覆盖所有局部场。
- 深半导体 C4b（二极管/三极管完整玩法与物理模型）仍待专门设计。

## 客户端 / 验证

- `docs/current_status` 的当前事实文档是本轮治理新增，需要后续随代码变更持续维护。
- Web/Voxia/Bevy 三条客户端的验收角色需要在后续 PR 说明中持续区分，避免把 Voxia 实跑资料误当协议唯一 oracle。
- 本轮文档治理没有跑测试；它是文档整理，不证明运行时状态变化。
