# 批量热内核

分类：Global system。Rustler 0.37.3 DirtyCpu 接收 World 冻结的节点与接触索引，
返回不可变温度、HP、余能与能量账；不保存世界、不发消息、不写数据库。
活动种子变化时在当前 50 ms 步末返回，让 World 更新六邻域后继续本批剩余步。
测试以现有 Elixir 单步公式及 World 活动前沿为对照。部署只更新本库，不改变世界生成器身份。
