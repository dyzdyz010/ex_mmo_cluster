[package]
name = "scene_ops"
version = "0.1.0"
authors = []
edition = "2018"

[lib]
name = "scene_ops"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.36.1"
nalgebra = "0.31"
rapier3d-f64 = { version = "0.16", features = [ "simd-stable", "parallel" ] }